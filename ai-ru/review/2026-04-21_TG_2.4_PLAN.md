# TG-бот 2.4 — deep-link onboarding: Implementation Plan

**Goal:** Убрать shareContact-шаг для пользователей, пришедших из мобилки. Signed start-token, backend генерация + проверка, mobile-endpoint + UI update.

**Спек:** [2026-04-21_TG_2.4_DEEP_LINK.md](2026-04-21_TG_2.4_DEEP_LINK.md).

---

## Файловая структура

### Create (backend main)

```
backend/src/main/java/ru/car/service/message/telegram/auth/
└── TelegramStartTokenService.java                   новый

backend/src/main/java/ru/car/controller/
└── TelegramAuthController.java                      новый

backend/src/main/java/ru/car/dto/
└── TelegramStartUrlDto.java                         новый { url: String }
```

### Modify (backend main)

- `TelegramRouter.java` — `extractAuthText` понимает `/start <payload>`.
- `TelegramAuthorizationService.java` — ветка deep-link (использует `TelegramStartTokenService`).
- `i18n/telegram_ru.properties` — 2 новых ключа.
- `UserService.java` — если нет `findById(Long)` — добавить.

### Create (backend test)

```
backend/src/test/java/ru/car/service/message/telegram/auth/
├── TelegramStartTokenServiceTest.java               новый
```

### Modify (backend test)

- `TelegramAuthorizationServiceTest.java` — кейсы на валидный токен, просроченный, tampered.
- `TelegramRouterTest.java` — `/start <payload>` проходит в auth с ободранным префиксом.

### Modify (mobile)

- `mobile/lib/repository/NotificationsRepository.dart` — `getTelegramStartUrl()`.
- `mobile/lib/controllers/notification_settings_controller.dart` — `observerOnResume`, метод `launchTelegramBot()`.
- `mobile/lib/screens/notifications_screen/notification_settings_screen.dart` — кнопка вызывает новый метод контроллера.

---

## Milestones

| # | Цель | Задачи |
|---|------|--------|
| M1 | Token service + tests | 1 |
| M2 | Router /start parsing | 2 |
| M3 | Auth service handles token | 3 |
| M4 | Controller + DTO + endpoint | 4 |
| M5 | Mobile integration | 5 |
| M6 | Docs + merge | 6–7 |

---

## Task 1: `TelegramStartTokenService`

**Step 1: test**

```java
@SpringJUnitConfig
class TelegramStartTokenServiceTest {
    TelegramStartTokenService svc = new TelegramStartTokenService("c2VjcmV0LWJhc2U2NC1rZXkteWVhaA==");

    @Test void signAndVerify_roundTrip() {
        String token = svc.sign(42L, Duration.ofMinutes(5));
        assertThat(svc.verify(token)).contains(42L);
    }

    @Test void verify_tamperedPayload_returnsEmpty() {
        String token = svc.sign(42L, Duration.ofMinutes(5));
        // подменить первый символ в payload (до '_')
        String tampered = "X" + token.substring(1);
        assertThat(svc.verify(tampered)).isEmpty();
    }

    @Test void verify_tamperedSignature_returnsEmpty() {
        String token = svc.sign(42L, Duration.ofMinutes(5));
        String tampered = token.substring(0, token.length()-1) + "X";
        assertThat(svc.verify(tampered)).isEmpty();
    }

    @Test void verify_expired_returnsEmpty() {
        String token = svc.sign(42L, Duration.ofMillis(10));
        Thread.sleep(50);
        assertThat(svc.verify(token)).isEmpty();
    }

    @Test void verify_blank_returnsEmpty() {
        assertThat(svc.verify("")).isEmpty();
        assertThat(svc.verify(null)).isEmpty();
        assertThat(svc.verify("no_underscore")).isEmpty();
    }

    @Test void tokenFitsTelegramBudget() {
        String token = svc.sign(Long.MAX_VALUE, Duration.ofDays(7));
        assertThat(token.length()).isLessThanOrEqualTo(64);
        assertThat(token).matches("[A-Za-z0-9_-]+");
    }
}
```

**Step 2: impl**

```java
package ru.car.service.message.telegram.auth;

import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Service;

import javax.crypto.Mac;
import javax.crypto.spec.SecretKeySpec;
import java.nio.charset.StandardCharsets;
import java.security.MessageDigest;
import java.time.Duration;
import java.time.Instant;
import java.util.Base64;
import java.util.Optional;

@Service
public class TelegramStartTokenService {

    private static final int TRUNCATED_HMAC_BYTES = 10; // 80 bits → 14 base64url chars
    private static final Base64.Encoder B64 = Base64.getUrlEncoder().withoutPadding();
    private static final Base64.Decoder B64D = Base64.getUrlDecoder();

    private final byte[] secret;

    public TelegramStartTokenService(@Value("${token.signing.key}") String base64Key) {
        this.secret = Base64.getDecoder().decode(base64Key);
    }

    public String sign(long userId, Duration ttl) {
        long exp = Instant.now().plus(ttl).getEpochSecond();
        String payload = userId + ":" + exp;
        byte[] payloadBytes = payload.getBytes(StandardCharsets.UTF_8);
        byte[] sig = hmac(payloadBytes);
        byte[] truncated = new byte[TRUNCATED_HMAC_BYTES];
        System.arraycopy(sig, 0, truncated, 0, TRUNCATED_HMAC_BYTES);
        return B64.encodeToString(payloadBytes) + "_" + B64.encodeToString(truncated);
    }

    public Optional<Long> verify(String token) {
        if (token == null || token.isBlank()) return Optional.empty();
        int sep = token.indexOf('_');
        if (sep < 0) return Optional.empty();
        try {
            byte[] payloadBytes = B64D.decode(token.substring(0, sep));
            byte[] sigBytes = B64D.decode(token.substring(sep + 1));
            if (sigBytes.length != TRUNCATED_HMAC_BYTES) return Optional.empty();
            byte[] expectedSig = hmac(payloadBytes);
            byte[] expectedTrunc = new byte[TRUNCATED_HMAC_BYTES];
            System.arraycopy(expectedSig, 0, expectedTrunc, 0, TRUNCATED_HMAC_BYTES);
            if (!MessageDigest.isEqual(sigBytes, expectedTrunc)) return Optional.empty();
            String payload = new String(payloadBytes, StandardCharsets.UTF_8);
            int col = payload.indexOf(':');
            if (col < 0) return Optional.empty();
            long userId = Long.parseLong(payload.substring(0, col));
            long exp = Long.parseLong(payload.substring(col + 1));
            if (Instant.now().getEpochSecond() > exp) return Optional.empty();
            return Optional.of(userId);
        } catch (IllegalArgumentException | IndexOutOfBoundsException e) {
            return Optional.empty();
        }
    }

    private byte[] hmac(byte[] data) {
        try {
            Mac mac = Mac.getInstance("HmacSHA256");
            mac.init(new SecretKeySpec(secret, "HmacSHA256"));
            return mac.doFinal(data);
        } catch (Exception e) {
            throw new IllegalStateException("HMAC init failed", e);
        }
    }
}
```

**Step 3/4: run tests, commit**

```bash
cd backend && ./gradlew test --tests "ru.car.service.message.telegram.auth.TelegramStartTokenServiceTest"
git commit -m "feat(tg-bot): TelegramStartTokenService — HMAC-signed deep-link tokens"
```

---

## Task 2: Router `/start <payload>` parsing

**Step 1: test** — расширить существующий `TelegramRouterTest` либо добавить новый кейс в `TelegramAuthorizationServiceTest`. Тест: `router.route(textUpdate("/start SOMETOKEN"))` → `authService.handle(chatId, "SOMETOKEN")` (без `/start `).

**Step 2: impl** — в `TelegramRouter.extractAuthText`:

```java
if (message.getText() != null) {
    String text = message.getText();
    if (text.startsWith("/start ")) {
        return text.substring("/start ".length()).trim();
    }
    if ("/start".equals(text.trim())) {
        return "";  // plain /start — без payload, падает в контакт-flow
    }
    return text;
}
```

**Step 3/4: tests + commit** `feat(tg-bot): router parses /start <payload>`

---

## Task 3: `TelegramAuthorizationService` ветка deep-link

**Step 1: test**

```java
@Test
void validStartToken_bindsWithoutContact() {
    User user = new User(); user.setId(42L);
    when(startTokenService.verify("valid.token")).thenReturn(Optional.of(42L));
    when(userService.findById(42L)).thenReturn(Optional.of(user));
    when(settingRepository.existsByTelegramDialogId(100L)).thenReturn(false);

    SceneOutput out = service.handle(100L, "valid.token");

    verify(settingRepository).updateTelegramDialogIdByUserId(42L, 100L);
    assertThat(out.text()).contains("Готово");
    assertThat(out.replyKeyboard()).isInstanceOf(ReplyKeyboardRemove.class);
}

@Test
void expiredStartToken_fallbackToContactFlow() {
    when(startTokenService.verify("expired.token")).thenReturn(Optional.empty());
    when(settingRepository.existsByTelegramDialogId(100L)).thenReturn(false);

    SceneOutput out = service.handle(100L, "expired.token");

    assertThat(out.text()).contains("Введите телефон");
}

@Test
void validTokenButUserMissing_rendersNotFoundMessage() {
    when(startTokenService.verify("orphan.token")).thenReturn(Optional.of(999L));
    when(userService.findById(999L)).thenReturn(Optional.empty());

    SceneOutput out = service.handle(100L, "orphan.token");

    assertThat(out.text()).contains("не найден");
}
```

**Step 2: impl**

```java
// Inject TelegramStartTokenService + UserService.findById
public SceneOutput handle(long chatId, String text) {
    if (settingRepository.existsByTelegramDialogId(chatId)) {
        return SceneOutput.send(messages.get("tg.auth.already_linked"), contactKeyboard());
    }
    if (text != null && !text.isBlank() && !MessageUtils.isValidPhone(text)) {
        Optional<Long> userIdOpt = startTokenService.verify(text);
        if (userIdOpt.isPresent()) {
            Optional<User> user = userService.findById(userIdOpt.get());
            if (user.isPresent()) {
                settingRepository.updateTelegramDialogIdByUserId(user.get().getId(), chatId);
                return SceneOutput.send(messages.get("tg.auth.welcome_deep_link"),
                    ReplyKeyboardRemove.builder().removeKeyboard(true).build());
            }
            return SceneOutput.send(messages.get("tg.auth.token_user_not_found"), contactKeyboard());
        }
    }
    // рез-классический phone flow
    if (!MessageUtils.isValidPhone(text)) { ... }
    // ...
}
```

Добавить `UserService.findById(Long) : Optional<User>` если нет.

**Step 3/4: commit** `feat(tg-bot): auth handles signed /start token (bind without contact)`

---

## Task 4: `TelegramAuthController` + DTO + endpoint

**Step 1: test** — integration test с MockMvc (или интеграция через `@SpringBootTest`). Проверить что зарегистрирован, требует JWT.

**Step 2: impl**

```java
package ru.car.dto;
@Data @Builder @AllArgsConstructor @NoArgsConstructor
public class TelegramStartUrlDto { private String url; }
```

```java
package ru.car.controller;

@RestController
@RequestMapping("api")
public class TelegramAuthController {

    private final TelegramStartTokenService tokenService;
    private final TelegramProperties tgProps;
    private final AuthService authService;

    public TelegramAuthController(TelegramStartTokenService tokenService,
                                   TelegramProperties tgProps,
                                   AuthService authService) {
        this.tokenService = tokenService;
        this.tgProps = tgProps;
        this.authService = authService;
    }

    @PostMapping("telegram.get_start_url")
    public MobileRs<TelegramStartUrlDto> getStartUrl() {
        Long userId = authService.getUserId();
        String token = tokenService.sign(userId, Duration.ofMinutes(15));
        String url = "https://t.me/" + tgProps.getBot() + "?start=" + token;
        return MobileRs.<TelegramStartUrlDto>builder()
            .result("true").params(TelegramStartUrlDto.builder().url(url).build()).build();
    }
}
```

**Step 3/4: commit** `feat(tg-bot): POST /api/telegram.get_start_url`

---

## Task 5: Mobile — repository + controller + screen

**Step 1: repository method** — в `NotificationsRepository`:

```dart
Future<String?> getTelegramStartUrl() async {
    final token = await storage.read(key: 'access_token');
    final response = await post(
        '$MAIN_URL/api/telegram.get_start_url',
        {'params': {}},
        headers: {'Authorization': 'Bearer $token', 'Content-Type': 'application/json'},
    );
    if (response.statusCode == 200 && response.body['params']?['url'] != null) {
        return response.body['params']['url'];
    }
    return null;
}
```

**Step 2: controller** — в `notification_settings_controller.dart`:

```dart
// add WidgetsBindingObserver
class NotificationSettingsController extends GetxController with WidgetsBindingObserver {
  @override void onInit() { super.onInit(); WidgetsBinding.instance.addObserver(this); /* ... */ }
  @override void onClose() { WidgetsBinding.instance.removeObserver(this); super.onClose(); }
  @override void didChangeAppLifecycleState(AppLifecycleState state) {
    if (state == AppLifecycleState.resumed) { fetchNotificationSettings(); }
  }

  Future<void> launchTelegramBot() async {
    final url = await repo.getTelegramStartUrl();
    if (url != null) {
      await Utils.openUrl(url);
    } else {
      await Utils.openUrl(TELEGRAM_BOT_URL);  // fallback
    }
  }
}
```

**Step 3: screen** — в `notification_settings_screen.dart` кнопка `onPressed: () { Get.back(); ctrl.launchTelegramBot(); }`.

**Step 4: commit** `feat(mobile): use backend-signed deep-link for Telegram onboarding`

---

## Task 6: Docs + tests → pass

- Обновить `F8_TELEGRAM_BOT.md` (добавить сценарий deep-link в списке сценариев).
- Обновить `BF1_TELEGRAM_BOT_UI.md` (2.4 ✅).
- Обновить эпик (2.4 ✅ + история).
- `./gradlew test` — PASS.

```bash
git commit -m "docs: close Phase 2.4 — deep-link onboarding"
```

---

## Task 7: Merge + push

```bash
git checkout main && git merge --no-ff feature/tg-bot-2.4 -m "Merge branch 'feature/tg-bot-2.4' — Phase 2.4 deep-link onboarding"
git push origin main
git branch -d feature/tg-bot-2.4
```

---

## Сводка

- Новых .java: 3 main (token service, controller, DTO) + 1 test = 4.
- Модифицированных backend: 3 (router, auth service, i18n).
- Mobile: 3 файла.
- Новых миграций: нет.
- Новых зависимостей: нет (HMAC уже есть в JDK).
