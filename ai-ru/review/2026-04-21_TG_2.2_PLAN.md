# TG-бот 2.2 — базовые экраны: Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Реализовать 6 новых сцен Telegram-бота (Home / QR-list / QR-details / Notifications / Settings / Profile) с HTML-форматированием, эмодзи, inline edit-in-place навигацией и PNG-доставкой QR-кодов.

**Architecture:** Наращиваем Scene-архитектуру 2.1 — 6 новых `@Component implements TelegramScene` + расширение `SceneOutput` полем `photo` + новый метод `TelegramTransport.sendPhoto`. Все существующие компоненты (router, renderer, registry, messages, callback data, auth) переиспользуются. Легаси reply-keyboard убирается; весь UX на inline. Counters и фильтры реализуются stateless через callback_data.

**Tech Stack:** Java 17, Spring Boot 3, telegrambots 6.x, JUnit 5, Mockito, AssertJ, ZXing (для PNG QR через существующий `QrUtils.generateQRCodeImage`).

**Спек:** [2026-04-21_TG_2.2_BASIC_SCREENS.md](2026-04-21_TG_2.2_BASIC_SCREENS.md). Эпик: [2026-04-21_TELEGRAM_EPIC.md](2026-04-21_TELEGRAM_EPIC.md).

---

## File Structure

### Create (main)

```
backend/src/main/java/ru/car/service/message/telegram/scene/impl/
├── HomeScene.java                           заменяет HomeMenuScene
├── QrDetailsScene.java                      новый
├── NotificationListScene.java               новый
├── NotificationSettingsScene.java           новый
└── ProfileScene.java                        новый
```

### Modify (main)

- `backend/src/main/java/ru/car/service/message/telegram/transport/TelegramTransport.java` — добавить `sendPhoto`.
- `backend/src/main/java/ru/car/service/message/telegram/TelegramBotService.java` — реализовать `sendPhoto`.
- `backend/src/main/java/ru/car/service/message/telegram/scene/SceneOutput.java` — добавить поля `photo`, `caption` + фабрики.
- `backend/src/main/java/ru/car/service/message/telegram/render/TelegramRenderer.java` — ветка dispatchPhoto.
- `backend/src/main/java/ru/car/service/message/telegram/scene/TelegramScene.java` — добавить `parentKey()` default.
- `backend/src/main/java/ru/car/service/message/telegram/router/TelegramRouter.java` — обработать `:back` callback через parentKey + после auth рендерить Home.
- `backend/src/main/java/ru/car/service/message/telegram/auth/TelegramAuthorizationService.java` — welcome возвращает `ReplyKeyboardRemove`.
- `backend/src/main/java/ru/car/service/message/telegram/scene/impl/QrListScene.java` — полностью переписать (HTML + карточки + переход в details).
- `backend/src/main/java/ru/car/service/message/telegram/scene/impl/TemporaryQrScene.java` — отправлять PNG через sendPhoto с caption.
- `backend/src/main/java/ru/car/util/MessageUtils.java` — добавить `escapeHtml`, `formatPhone`.
- `backend/src/main/resources/i18n/telegram_ru.properties` — новые ключи (~40).
- `backend/src/main/java/ru/car/service/NotificationService.java` — `countUnreadByUserId`, `findForBot`.
- `backend/src/main/java/ru/car/service/QrService.java` — `countByUserId`, `disable(UUID)`.
- `backend/src/main/java/ru/car/service/NotificationSettingService.java` — `toggleChannel`.
- `backend/src/main/java/ru/car/repository/NotificationSettingRepository.java` — `clearTelegramLink(userId)`.
- `backend/src/main/java/ru/car/repository/NotificationRepository.java` — `findPageByUserIdAndQrId` и/или параметризованный поиск.

### Delete (main)

- `backend/src/main/java/ru/car/service/message/telegram/scene/impl/HomeMenuScene.java` — заменён `HomeScene`.

### Create (test) / Modify (test)

```
backend/src/test/java/ru/car/service/message/telegram/scene/impl/
├── HomeSceneTest.java                       новый
├── QrDetailsSceneTest.java                  новый
├── NotificationListSceneTest.java           новый
├── NotificationSettingsSceneTest.java       новый
├── ProfileSceneTest.java                    новый
├── QrListSceneTest.java                     переписать
├── TemporaryQrSceneTest.java                обновить assertion
└── HomeMenuSceneTest.java                   удалить
```

Modify: `TelegramRendererTest`, `CallbackDataTest`, `TelegramRouterTest`, `TelegramAuthorizationServiceTest`, `NotificationServiceTest`, `QrServiceTest`.

---

## Tasks

### Task 1: `MessageUtils` — `escapeHtml` + `formatPhone`

**Files:**
- Modify: `backend/src/main/java/ru/car/util/MessageUtils.java`
- Modify: `backend/src/test/java/ru/car/util/MessageUtilsTest.java` (создать если нет)

- [ ] **Step 1: Write failing tests**

Add to `MessageUtilsTest.java`:

```java
@Test
void escapeHtml_escapesAngleBracketsAndAmpersand() {
    assertThat(MessageUtils.escapeHtml("<b>A & B</b>")).isEqualTo("&lt;b&gt;A &amp; B&lt;/b&gt;");
}

@Test
void escapeHtml_null_returnsEmptyString() {
    assertThat(MessageUtils.escapeHtml(null)).isEqualTo("");
}

@Test
void formatPhone_russianNumber() {
    assertThat(MessageUtils.formatPhone("79313178898")).isEqualTo("+7 931 317-88-98");
}

@Test
void formatPhone_invalid_returnsOriginal() {
    assertThat(MessageUtils.formatPhone("abc")).isEqualTo("abc");
    assertThat(MessageUtils.formatPhone(null)).isEqualTo("");
}
```

If test class doesn't exist, create with:

```java
package ru.car.util;

import org.junit.jupiter.api.Test;
import static org.assertj.core.api.Assertions.assertThat;

class MessageUtilsTest {
    // tests above
}
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd backend && ./gradlew test --tests "ru.car.util.MessageUtilsTest"`
Expected: FAIL (methods don't exist).

- [ ] **Step 3: Add methods**

In `MessageUtils.java` add:

```java
public static String escapeHtml(String raw) {
    if (raw == null) {
        return "";
    }
    return raw.replace("&", "&amp;")
              .replace("<", "&lt;")
              .replace(">", "&gt;");
}

public static String formatPhone(String phone) {
    if (phone == null) {
        return "";
    }
    if (phone.length() != 11 || !phone.startsWith("7") || !phone.matches("\\d+")) {
        return phone;
    }
    return "+" + phone.charAt(0) + " " + phone.substring(1, 4) + " " + phone.substring(4, 7)
            + "-" + phone.substring(7, 9) + "-" + phone.substring(9, 11);
}
```

- [ ] **Step 4: Verify tests pass**

Run: `cd backend && ./gradlew test --tests "ru.car.util.MessageUtilsTest"`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add backend/src/main/java/ru/car/util/MessageUtils.java backend/src/test/java/ru/car/util/MessageUtilsTest.java
git commit -m "feat(tg-bot): add MessageUtils.escapeHtml + formatPhone helpers"
```

---

### Task 2: Extend `SceneOutput` with photo/caption

**Files:**
- Modify: `backend/src/main/java/ru/car/service/message/telegram/scene/SceneOutput.java`

- [ ] **Step 1: Replace file content**

Replace `SceneOutput.java` entirely:

```java
package ru.car.service.message.telegram.scene;

import org.telegram.telegrambots.meta.api.objects.InputFile;
import org.telegram.telegrambots.meta.api.objects.replykeyboard.InlineKeyboardMarkup;
import org.telegram.telegrambots.meta.api.objects.replykeyboard.ReplyKeyboard;

public record SceneOutput(
        String text,
        InlineKeyboardMarkup inlineKeyboard,
        ReplyKeyboard replyKeyboard,
        boolean editInPlace,
        String parseMode,
        InputFile photo,
        String caption
) {
    public static SceneOutput send(String text, ReplyKeyboard replyKeyboard) {
        return new SceneOutput(text, null, replyKeyboard, false, null, null, null);
    }

    public static SceneOutput sendWithInline(String text, InlineKeyboardMarkup inline) {
        return new SceneOutput(text, inline, null, false, null, null, null);
    }

    public static SceneOutput sendHtml(String text, InlineKeyboardMarkup inline) {
        return new SceneOutput(text, inline, null, false, "HTML", null, null);
    }

    public static SceneOutput editText(String text, InlineKeyboardMarkup inline) {
        return new SceneOutput(text, inline, null, true, null, null, null);
    }

    public static SceneOutput editHtml(String text, InlineKeyboardMarkup inline) {
        return new SceneOutput(text, inline, null, true, "HTML", null, null);
    }

    public static SceneOutput editMarkup(InlineKeyboardMarkup inline) {
        return new SceneOutput(null, inline, null, true, null, null, null);
    }

    public static SceneOutput photo(InputFile photo, String caption) {
        return new SceneOutput(null, null, null, false, "HTML", photo, caption);
    }

    public static SceneOutput noop() {
        return new SceneOutput(null, null, null, false, null, null, null);
    }
}
```

- [ ] **Step 2: Verify compile**

Run: `cd backend && ./gradlew compileJava`
Expected: FAIL — existing `new SceneOutput(...)` callsites use 5-arg constructor. Let me check...

Actually all existing code uses the static factories — `send()`, `editMarkup()`, etc. — which we preserved. So compile should pass.

Run: `cd backend && ./gradlew compileJava compileTestJava`
Expected: SUCCESS.

- [ ] **Step 3: Run all telegram tests**

Run: `cd backend && ./gradlew test --tests "ru.car.service.message.telegram.*"`
Expected: all PASS (previous tests used factories).

- [ ] **Step 4: Commit**

```bash
git add backend/src/main/java/ru/car/service/message/telegram/scene/SceneOutput.java
git commit -m "feat(tg-bot): extend SceneOutput with photo/caption + HTML factories"
```

---

### Task 3: `TelegramTransport.sendPhoto` + BotService impl

**Files:**
- Modify: `backend/src/main/java/ru/car/service/message/telegram/transport/TelegramTransport.java`
- Modify: `backend/src/main/java/ru/car/service/message/telegram/TelegramBotService.java`

- [ ] **Step 1: Add sendPhoto to interface**

In `TelegramTransport.java` add method:

```java
void sendPhoto(org.telegram.telegrambots.meta.api.methods.send.SendPhoto photo);
```

Refactor imports so `SendPhoto` is imported at top.

- [ ] **Step 2: Implement in TelegramBotService**

In `TelegramBotService.java` add override (next to `sendDocument`):

```java
@Override
public void sendPhoto(SendPhoto photo) {
    try {
        execute(photo);
    } catch (TelegramApiException e) {
        log.error("Can't send telegram photo", e);
        throw new MessageNotSendException(e);
    }
}
```

Add import `import org.telegram.telegrambots.meta.api.methods.send.SendPhoto;`.

- [ ] **Step 3: Verify compile**

Run: `cd backend && ./gradlew compileJava`
Expected: SUCCESS.

- [ ] **Step 4: Commit**

```bash
git add backend/src/main/java/ru/car/service/message/telegram/transport/TelegramTransport.java \
        backend/src/main/java/ru/car/service/message/telegram/TelegramBotService.java
git commit -m "feat(tg-bot): add TelegramTransport.sendPhoto"
```

---

### Task 4: `TelegramRenderer.dispatchPhoto`

**Files:**
- Modify: `backend/src/main/java/ru/car/service/message/telegram/render/TelegramRenderer.java`
- Modify: `backend/src/test/java/ru/car/service/message/telegram/render/TelegramRendererTest.java`

- [ ] **Step 1: Write failing tests**

Add to `TelegramRendererTest.java`:

```java
@Test
void dispatchesPhoto_whenSceneOutputHasPhoto() {
    InputFile inputFile = new InputFile(new java.io.ByteArrayInputStream(new byte[]{1,2,3}), "qr.png");
    SceneOutput output = SceneOutput.photo(inputFile, "Audi Q5");

    renderer.dispatch(output, 42L, null);

    org.mockito.ArgumentCaptor<org.telegram.telegrambots.meta.api.methods.send.SendPhoto> captor =
        org.mockito.ArgumentCaptor.forClass(org.telegram.telegrambots.meta.api.methods.send.SendPhoto.class);
    verify(transport).sendPhoto(captor.capture());
    assertThat(captor.getValue().getCaption()).isEqualTo("Audi Q5");
    assertThat(captor.getValue().getParseMode()).isEqualTo("HTML");
}
```

- [ ] **Step 2: Run test → fail**

Run: `cd backend && ./gradlew test --tests "ru.car.service.message.telegram.render.TelegramRendererTest.dispatchesPhoto*"`
Expected: FAIL.

- [ ] **Step 3: Update dispatch method**

In `TelegramRenderer.java` replace `dispatch` method:

```java
public void dispatch(SceneOutput output, long chatId, Message editTargetOrNull) {
    if (output == null) {
        return;
    }
    if (output.photo() != null) {
        dispatchPhoto(output, chatId);
        return;
    }
    if (output.editInPlace() && editTargetOrNull != null) {
        dispatchEdit(output, editTargetOrNull);
        return;
    }
    if (output.text() == null && output.inlineKeyboard() == null && output.replyKeyboard() == null) {
        return;
    }
    dispatchSend(output, chatId);
}

private void dispatchPhoto(SceneOutput output, long chatId) {
    org.telegram.telegrambots.meta.api.methods.send.SendPhoto.SendPhotoBuilder<?, ?> builder =
        org.telegram.telegrambots.meta.api.methods.send.SendPhoto.builder()
            .chatId(chatId)
            .photo(output.photo());
    if (output.caption() != null) {
        builder.caption(output.caption());
    }
    if (output.parseMode() != null) {
        builder.parseMode(output.parseMode());
    }
    transport.sendPhoto(builder.build());
}
```

Move `SendPhoto` to top imports.

- [ ] **Step 4: Run tests → pass**

Run: `cd backend && ./gradlew test --tests "ru.car.service.message.telegram.render.TelegramRendererTest"`
Expected: all PASS.

- [ ] **Step 5: Commit**

```bash
git add backend/src/main/java/ru/car/service/message/telegram/render/TelegramRenderer.java \
        backend/src/test/java/ru/car/service/message/telegram/render/TelegramRendererTest.java
git commit -m "feat(tg-bot): add dispatchPhoto branch to TelegramRenderer"
```

---

### Task 5: i18n properties — новые ключи

**Files:**
- Modify: `backend/src/main/resources/i18n/telegram_ru.properties`

- [ ] **Step 1: Append new keys**

Add to end of `telegram_ru.properties`:

```properties

# === 2.2: new screens ===

# home
tg.home.title=🏠 <b>Car ID</b>
tg.home.btn.notifications=🔔 Уведомления · {0} новых
tg.home.btn.notifications_zero=🔔 Уведомления
tg.home.btn.qrs=🚘 Мои метки · {0}
tg.home.btn.qrs_zero=🚘 Мои метки
tg.home.btn.temp_qr=➕ Временный QR
tg.home.btn.settings=⚙️ Настройки
tg.home.btn.profile=👤 Профиль

# qr list (v2)
tg.qr.list.title=🚘 <b>Мои метки</b>
tg.qr.list.card.active={0} <b>{1}</b> · №{2}\n📅 С {3}, {4} уведомлений
tg.qr.list.card.temp=🕐 <b>Временная</b> · истечёт в {0}
tg.qr.list.card.disabled=⏸ <b>{0}</b> · №{1}
tg.qr.list.empty_v2=🚘 <b>Мои метки</b>\n\nУ Вас пока нет меток. Закажите постоянный QR на Ozon или создайте временный для теста.
tg.qr.list.btn.ozon=🛒 Заказать на Ozon

# qr details
tg.qr.details.title={0} <b>{1}</b>
tg.qr.details.fields=<b>Номер:</b> №{0}\n<b>Статус:</b> {1}\n<b>Активирована:</b> {2}\n<b>Уведомлений:</b> {3} ({4} непрочитанных)
tg.qr.details.btn.show=🖼 Показать QR
tg.qr.details.btn.history=📜 История уведомлений
tg.qr.details.btn.disable=⏸ Отключить
tg.qr.details.confirm.disable=⚠️ <b>Отключить метку?</b>\n<i>Уведомления по ней перестанут приходить.</i>
tg.qr.details.btn.back_to_list=← К меткам
tg.qr.details.status.active=Активна
tg.qr.details.status.disabled=Отключена
tg.qr.details.status.temp=Временная
tg.qr.details.status.new=Не активирована

# notifications list
tg.notif.list.title=🔔 <b>Уведомления</b>
tg.notif.list.tabs_all=<b>Все</b> | Непрочитанные
tg.notif.list.tabs_unread=Все | <b>Непрочитанные</b>
tg.notif.list.item.unread=🔴 {0} · <b>{1}</b>\n   {2}
tg.notif.list.item.read=⚪ {0} · <b>{1}</b>\n   {2}
tg.notif.list.btn.item_unread=🔴 {0} · {1}
tg.notif.list.btn.item_read=⚪ {0} · {1}
tg.notif.list.btn.tab_all=📑 Все
tg.notif.list.btn.tab_unread=🔴 Непрочитанные
tg.notif.list.pagination=‹ {0}/{1} ›
tg.notif.list.empty=🔔 <b>Уведомления</b>\n\nНет уведомлений.

# settings
tg.settings.title=⚙️ <b>Настройки уведомлений</b>
tg.settings.body=🔔 Push: <b>{0}</b>\n📞 Звонок робота: <b>{1}</b>\n✈️ Telegram: <b>{2}</b>
tg.settings.btn.toggle_push_on=🔔 Push · выкл
tg.settings.btn.toggle_push_off=🔔 Push · вкл
tg.settings.btn.toggle_call_on=📞 Звонок · выкл
tg.settings.btn.toggle_call_off=📞 Звонок · вкл
tg.settings.btn.toggle_telegram_on=✈️ Telegram · выкл
tg.settings.btn.toggle_telegram_off=✈️ Telegram · вкл
tg.settings.on=включены
tg.settings.off=выключены
tg.settings.on_short=вкл
tg.settings.off_short=выкл

# profile
tg.profile.title=👤 <b>Профиль</b>
tg.profile.body=<b>Телефон:</b> {0}\n<b>Роль:</b> {1}
tg.profile.role_user=Пользователь
tg.profile.role_admin=Администратор
tg.profile.btn.logout=🚪 Выйти
tg.profile.btn.delete=🗑 Удалить аккаунт
tg.profile.confirm.logout=⚠️ <b>Выйти из бота?</b>\n<i>Уведомления в Telegram перестанут приходить. Войти снова через /start.</i>
tg.profile.confirm.delete=⚠️ <b>Удалить аккаунт?</b>\n<i>Все метки будут удалены и непригодны для дальнейшего использования.</i>
tg.profile.logout.done=Вы отключены от Telegram. Войти снова — через /start.
tg.profile.delete.done=Аккаунт удалён.

# common
tg.common.back=← Назад
tg.common.home=🏠 Главное
tg.common.confirm.yes=❌ Подтвердить
tg.common.confirm.cancel=↩ Отмена

# temp qr (v2 — PNG with caption)
tg.temp_qr.caption=Для Вас создан временный QR.\nПройдите по ссылке: {0}\nИстекает через 1 ч.

# unknown command (keeps tg.home.unknown_command from 2.1)
tg.unknown.btn.open_home=🏠 Главное меню
```

- [ ] **Step 2: Verify load + add TelegramMessagesTest key checks**

In `TelegramMessagesTest.java` add a smoke-test that iterates a sample of keys:

```java
@Test
void all2_2KeysPresent() {
    String[] required = {
        "tg.home.title", "tg.home.btn.notifications", "tg.home.btn.qrs", "tg.home.btn.temp_qr",
        "tg.home.btn.settings", "tg.home.btn.profile",
        "tg.qr.list.title", "tg.qr.list.card.active", "tg.qr.list.empty_v2",
        "tg.qr.details.title", "tg.qr.details.fields", "tg.qr.details.confirm.disable",
        "tg.notif.list.title", "tg.notif.list.pagination", "tg.notif.list.empty",
        "tg.settings.title", "tg.settings.body", "tg.settings.on",
        "tg.profile.title", "tg.profile.body", "tg.profile.confirm.delete",
        "tg.common.back", "tg.common.home", "tg.common.confirm.yes",
        "tg.temp_qr.caption"
    };
    for (String key : required) {
        assertThat(messages.get(key)).as("key %s", key).isNotBlank();
    }
}
```

- [ ] **Step 3: Run tests**

Run: `cd backend && ./gradlew test --tests "ru.car.service.message.telegram.render.TelegramMessagesTest"`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add backend/src/main/resources/i18n/telegram_ru.properties \
        backend/src/test/java/ru/car/service/message/telegram/render/TelegramMessagesTest.java
git commit -m "feat(tg-bot): i18n keys for 2.2 screens"
```

---

### Task 6: Service counts — `NotificationService.countUnreadByUserId` + `QrService.countByUserId`

**Files:**
- Modify: `backend/src/main/java/ru/car/service/NotificationService.java`
- Modify: `backend/src/main/java/ru/car/service/QrService.java`
- Modify: `backend/src/test/java/ru/car/service/NotificationServiceTest.java`
- Modify: `backend/src/test/java/ru/car/service/QrServiceTest.java`

- [ ] **Step 1: Write failing tests (NotificationService)**

Add to `NotificationServiceTest.java`:

```java
@Test
void countUnreadByUserId_delegatesToRepo() {
    when(notificationRepository.findCountByUserIdAndStatus(1L, NotificationStatus.UNREAD))
        .thenReturn(5);
    assertThat(notificationService.countUnreadByUserId(1L)).isEqualTo(5);
}
```

- [ ] **Step 2: Write failing tests (QrService)**

Add to `QrServiceTest.java`:

```java
@Test
void countByUserId_countsAllExceptDeleted() {
    Qr active1 = new Qr(); active1.setStatus(QrStatus.ACTIVE);
    Qr active2 = new Qr(); active2.setStatus(QrStatus.ACTIVE);
    Qr temp = new Qr(); temp.setStatus(QrStatus.TEMPORARY);
    Qr deleted = new Qr(); deleted.setStatus(QrStatus.DELETED);
    when(qrRepository.findByUserId(42L)).thenReturn(java.util.List.of(active1, active2, temp, deleted));

    assertThat(qrService.countByUserId(42L)).isEqualTo(3);
}
```

- [ ] **Step 3: Run → fail**

Run: `cd backend && ./gradlew test --tests "ru.car.service.NotificationServiceTest.countUnreadByUserId*" --tests "ru.car.service.QrServiceTest.countByUserId*"`
Expected: FAIL.

- [ ] **Step 4: Add methods**

In `NotificationService.java` add:

```java
@Transactional(readOnly = true)
public int countUnreadByUserId(Long userId) {
    return notificationRepository.findCountByUserIdAndStatus(userId, NotificationStatus.UNREAD);
}
```

In `QrService.java` add:

```java
@Transactional(readOnly = true)
public int countByUserId(Long userId) {
    return (int) qrRepository.findByUserId(userId).stream()
            .filter(qr -> !QrStatus.DELETED.equals(qr.getStatus()))
            .count();
}
```

Imports: `ru.car.enums.QrStatus`, `ru.car.enums.NotificationStatus` (if not present).

- [ ] **Step 5: Run → pass**

Run: `cd backend && ./gradlew test --tests "ru.car.service.NotificationServiceTest" --tests "ru.car.service.QrServiceTest"`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add backend/src/main/java/ru/car/service/NotificationService.java \
        backend/src/main/java/ru/car/service/QrService.java \
        backend/src/test/java/ru/car/service/NotificationServiceTest.java \
        backend/src/test/java/ru/car/service/QrServiceTest.java
git commit -m "feat(tg-bot): add count methods for Home screen counters"
```

---

### Task 7: `QrService.disable(UUID)`

**Files:**
- Modify: `backend/src/main/java/ru/car/service/QrService.java`
- Modify: `backend/src/test/java/ru/car/service/QrServiceTest.java`

- [ ] **Step 1: Write failing test**

In `QrServiceTest.java`:

```java
@Test
void disable_setsStatusDeletedAndSaves() {
    UUID id = UUID.randomUUID();
    Qr qr = new Qr();
    qr.setId(id);
    qr.setStatus(QrStatus.ACTIVE);
    when(qrRepository.findById(id)).thenReturn(java.util.Optional.of(qr));
    when(qrRepository.save(any(Qr.class))).thenAnswer(inv -> inv.getArgument(0));

    qrService.disable(id);

    org.mockito.ArgumentCaptor<Qr> captor = org.mockito.ArgumentCaptor.forClass(Qr.class);
    verify(qrRepository).save(captor.capture());
    assertThat(captor.getValue().getStatus()).isEqualTo(QrStatus.DELETED);
}
```

Check `QrRepository` has `findById` + `save`. If not, use existing API:

```java
// Inspect QrRepository first. If findById/save are present — good.
// Otherwise replace with actual method names.
```

- [ ] **Step 2: Check QrRepository methods**

Run: `grep "public" backend/src/main/java/ru/car/repository/QrRepository.java | head -15`

Required: method to find Qr by id and method to persist status change. If existing methods are `findByIdOrThrowNotFound` and `updateStatus` — adapt test and impl.

- [ ] **Step 3: Implement disable**

In `QrService.java` add (adapting to actual repository API):

```java
@Transactional
public void disable(UUID id) {
    Qr qr = qrRepository.findById(id)
            .orElseThrow(() -> new NotFoundException("Qr not found: " + id));
    qr.setStatus(QrStatus.DELETED);
    qr.setUpdatedDate(java.time.LocalDateTime.now());
    qrRepository.save(qr);
}
```

If `qrRepository` doesn't have `findById/save` but has other names (like `findByIdOrThrowNotFound` and `update`), use those:

```java
@Transactional
public void disable(UUID id) {
    Qr qr = findByIdOrThrowNotFound(id);
    qr.setStatus(QrStatus.DELETED);
    qrRepository.update(qr);  // or whatever method exists
}
```

Import `NotFoundException` from appropriate package.

- [ ] **Step 4: Run → pass**

Run: `cd backend && ./gradlew test --tests "ru.car.service.QrServiceTest.disable*"`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add backend/src/main/java/ru/car/service/QrService.java \
        backend/src/test/java/ru/car/service/QrServiceTest.java
git commit -m "feat(tg-bot): add QrService.disable(UUID)"
```

---

### Task 8: `NotificationService.findForBot` (paged + filtered)

**Files:**
- Modify: `backend/src/main/java/ru/car/service/NotificationService.java`
- Modify: `backend/src/test/java/ru/car/service/NotificationServiceTest.java`
- Modify: `backend/src/main/java/ru/car/repository/NotificationRepository.java` (new repo method if needed)

- [ ] **Step 1: Check existing repo**

Run: `grep "findPage" backend/src/main/java/ru/car/repository/NotificationRepository.java`

Existing: `findPageByUserId(Long, PageParam)`, `findPageByUserIdAndStatus(Long, NotificationStatus, PageParam)`. Need one with QR-filter too.

- [ ] **Step 2: Add repo method**

In `NotificationRepository.java` add:

```java
private static final String SELECT_PAGE_BY_USER_ID_AND_QR_ID = 
    "SELECT n.* FROM notifications n left join qrs q on n.qr_id = q.id " +
    "WHERE q.user_id = :userId and n.qr_id = :qrId ORDER BY n.created_date DESC LIMIT :size OFFSET :offset";

private static final String SELECT_PAGE_BY_USER_ID_AND_STATUS_AND_QR_ID = 
    "SELECT n.* FROM notifications n left join qrs q on n.qr_id = q.id " +
    "WHERE q.user_id = :userId and n.status = :status and n.qr_id = :qrId " +
    "ORDER BY n.created_date DESC LIMIT :size OFFSET :offset";

public List<Notification> findPageByUserIdAndQrId(Long userId, UUID qrId, PageParam page) {
    MapSqlParameterSource params = new MapSqlParameterSource()
        .addValue("userId", userId)
        .addValue("qrId", qrId)
        .addValue("size", page.getSize())
        .addValue("offset", page.getPage() * page.getSize());
    return template.query(SELECT_PAGE_BY_USER_ID_AND_QR_ID, params, rowMapper);
}

public List<Notification> findPageByUserIdAndStatusAndQrId(Long userId, NotificationStatus status, UUID qrId, PageParam page) {
    MapSqlParameterSource params = new MapSqlParameterSource()
        .addValue("userId", userId)
        .addValue("status", status.name())
        .addValue("qrId", qrId)
        .addValue("size", page.getSize())
        .addValue("offset", page.getPage() * page.getSize());
    return template.query(SELECT_PAGE_BY_USER_ID_AND_STATUS_AND_QR_ID, params, rowMapper);
}
```

Use same style as existing `findPageByUserId`. Check exact pattern of SQL execution in neighbouring methods.

- [ ] **Step 3: Write failing service test**

In `NotificationServiceTest.java`:

```java
@Test
void findForBot_allTab_noQrFilter_delegatesToFindPageByUserId() {
    PageParam page = PageParam.builder().page(0).size(5).build();
    when(notificationRepository.findPageByUserId(42L, page))
        .thenReturn(java.util.List.of(new Notification()));

    var result = notificationService.findForBot(42L, "all", null, page);

    assertThat(result).hasSize(1);
}

@Test
void findForBot_unreadTab_filtersByStatus() {
    PageParam page = PageParam.builder().page(0).size(5).build();
    when(notificationRepository.findPageByUserIdAndStatus(42L, NotificationStatus.UNREAD, page))
        .thenReturn(java.util.List.of(new Notification()));

    var result = notificationService.findForBot(42L, "unread", null, page);

    assertThat(result).hasSize(1);
}

@Test
void findForBot_withQrFilter_delegatesToFindPageByUserIdAndQrId() {
    PageParam page = PageParam.builder().page(0).size(5).build();
    UUID qrId = UUID.randomUUID();
    when(notificationRepository.findPageByUserIdAndQrId(42L, qrId, page))
        .thenReturn(java.util.List.of(new Notification()));

    var result = notificationService.findForBot(42L, "all", qrId, page);

    assertThat(result).hasSize(1);
}
```

- [ ] **Step 4: Run → fail**

Run: `cd backend && ./gradlew test --tests "ru.car.service.NotificationServiceTest.findForBot*"`
Expected: FAIL (method doesn't exist).

- [ ] **Step 5: Implement in service**

In `NotificationService.java`:

```java
@Transactional(readOnly = true)
public List<Notification> findForBot(Long userId, String tab, UUID qrFilter, PageParam page) {
    boolean unread = "unread".equals(tab);
    if (qrFilter != null) {
        return unread
            ? notificationRepository.findPageByUserIdAndStatusAndQrId(userId, NotificationStatus.UNREAD, qrFilter, page)
            : notificationRepository.findPageByUserIdAndQrId(userId, qrFilter, page);
    }
    return unread
        ? notificationRepository.findPageByUserIdAndStatus(userId, NotificationStatus.UNREAD, page)
        : notificationRepository.findPageByUserId(userId, page);
}
```

Add imports for `Notification`, `PageParam`, `UUID`, `List`.

- [ ] **Step 6: Run → pass**

Run: `cd backend && ./gradlew test --tests "ru.car.service.NotificationServiceTest"`
Expected: PASS.

- [ ] **Step 7: Commit**

```bash
git add backend/src/main/java/ru/car/service/NotificationService.java \
        backend/src/main/java/ru/car/repository/NotificationRepository.java \
        backend/src/test/java/ru/car/service/NotificationServiceTest.java
git commit -m "feat(tg-bot): add NotificationService.findForBot paged + filtered"
```

---

### Task 9: `NotificationSettingService.toggleChannel`

**Files:**
- Modify: `backend/src/main/java/ru/car/service/NotificationSettingService.java`
- Modify: `backend/src/test/java/ru/car/service/NotificationSettingServiceTest.java` (create if absent)

- [ ] **Step 1: Inspect NotificationSettingService**

Run: `grep "public" backend/src/main/java/ru/car/service/NotificationSettingService.java | head`

Note existing methods; adapt to existing patterns (e.g. if there's `update(NotificationSetting)` method, use it).

- [ ] **Step 2: Write failing test**

Create or append to `NotificationSettingServiceTest.java`:

```java
package ru.car.service;

import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;
import ru.car.model.NotificationSetting;
import ru.car.repository.NotificationSettingRepository;

import static org.assertj.core.api.Assertions.assertThat;
import static org.mockito.ArgumentMatchers.any;
import static org.mockito.Mockito.when;

@ExtendWith(MockitoExtension.class)
class NotificationSettingServiceTest {

    @Mock NotificationSettingRepository repository;

    @InjectMocks NotificationSettingService service;

    @Test
    void toggleChannel_push_flipsBooleanAndSaves() {
        NotificationSetting setting = NotificationSetting.builder()
            .userId(42L).pushEnabled(true).callEnabled(false).telegramEnabled(true).active(true)
            .build();
        when(repository.findByUserId(42L)).thenReturn(setting);
        when(repository.update(any(NotificationSetting.class))).thenAnswer(i -> i.getArgument(0));

        NotificationSetting result = service.toggleChannel(42L, "push");

        assertThat(result.getPushEnabled()).isFalse();
        assertThat(result.getCallEnabled()).isFalse();
        assertThat(result.getTelegramEnabled()).isTrue();
    }

    @Test
    void toggleChannel_call_flipsAndSaves() {
        NotificationSetting setting = NotificationSetting.builder()
            .userId(42L).pushEnabled(true).callEnabled(false).telegramEnabled(true).active(true)
            .build();
        when(repository.findByUserId(42L)).thenReturn(setting);
        when(repository.update(any(NotificationSetting.class))).thenAnswer(i -> i.getArgument(0));

        NotificationSetting result = service.toggleChannel(42L, "call");

        assertThat(result.getCallEnabled()).isTrue();
    }

    @Test
    void toggleChannel_telegram_flipsAndSaves() {
        NotificationSetting setting = NotificationSetting.builder()
            .userId(42L).pushEnabled(true).callEnabled(false).telegramEnabled(true).active(true)
            .build();
        when(repository.findByUserId(42L)).thenReturn(setting);
        when(repository.update(any(NotificationSetting.class))).thenAnswer(i -> i.getArgument(0));

        NotificationSetting result = service.toggleChannel(42L, "telegram");

        assertThat(result.getTelegramEnabled()).isFalse();
    }

    @Test
    void toggleChannel_unknown_throws() {
        org.assertj.core.api.Assertions.assertThatThrownBy(() -> service.toggleChannel(42L, "xxx"))
            .isInstanceOf(IllegalArgumentException.class);
    }
}
```

- [ ] **Step 3: Run → fail**

Run: `cd backend && ./gradlew test --tests "ru.car.service.NotificationSettingServiceTest"`
Expected: FAIL.

- [ ] **Step 4: Implement method**

In `NotificationSettingService.java` add:

```java
@Transactional
public NotificationSetting toggleChannel(Long userId, String channel) {
    NotificationSetting setting = notificationSettingRepository.findByUserId(userId);
    switch (channel) {
        case "push" -> setting.setPushEnabled(!Boolean.TRUE.equals(setting.getPushEnabled()));
        case "call" -> setting.setCallEnabled(!Boolean.TRUE.equals(setting.getCallEnabled()));
        case "telegram" -> setting.setTelegramEnabled(!Boolean.TRUE.equals(setting.getTelegramEnabled()));
        default -> throw new IllegalArgumentException("Unknown channel: " + channel);
    }
    return notificationSettingRepository.update(setting);
}
```

Make sure field name matches (some projects use `repository`, others `notificationSettingRepository`).

- [ ] **Step 5: Run → pass**

Run: `cd backend && ./gradlew test --tests "ru.car.service.NotificationSettingServiceTest"`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add backend/src/main/java/ru/car/service/NotificationSettingService.java \
        backend/src/test/java/ru/car/service/NotificationSettingServiceTest.java
git commit -m "feat(tg-bot): add NotificationSettingService.toggleChannel"
```

---

### Task 10: `NotificationSettingRepository.clearTelegramLink`

**Files:**
- Modify: `backend/src/main/java/ru/car/repository/NotificationSettingRepository.java`

- [ ] **Step 1: Add SQL constant + method**

In `NotificationSettingRepository.java`:

```java
private static final String CLEAR_TELEGRAM_LINK = 
    "UPDATE notification_settings SET telegram_enabled = false, telegram_dialog_id = NULL WHERE user_id = :userId";

public void clearTelegramLink(Long userId) {
    MapSqlParameterSource params = new MapSqlParameterSource().addValue("userId", userId);
    template.update(CLEAR_TELEGRAM_LINK, params);
}
```

Use same style as other update methods; check exact JdbcTemplate field name.

- [ ] **Step 2: Build check**

Run: `cd backend && ./gradlew compileJava`
Expected: SUCCESS.

- [ ] **Step 3: Commit**

```bash
git add backend/src/main/java/ru/car/repository/NotificationSettingRepository.java
git commit -m "feat(tg-bot): add NotificationSettingRepository.clearTelegramLink"
```

---

### Task 11: `TelegramScene.parentKey` + router `:back` handling

**Files:**
- Modify: `backend/src/main/java/ru/car/service/message/telegram/scene/TelegramScene.java`
- Modify: `backend/src/main/java/ru/car/service/message/telegram/router/TelegramRouter.java`
- Modify: `backend/src/test/java/ru/car/service/message/telegram/router/TelegramRouterTest.java`

- [ ] **Step 1: Add parentKey default**

In `TelegramScene.java` add:

```java
/**
 * Ключ родительской сцены — используется роутером при получении callback `<scene>:back`.
 * По умолчанию возвращает "home" — большинство сцен возвращают на главный экран.
 */
default String parentKey() {
    return "home";
}
```

- [ ] **Step 2: Extend router**

In `TelegramRouter.java` modify `handleCallback`:

```java
private void handleCallback(TelegramUpdateContext ctx) {
    String raw = ctx.update().getCallbackQuery().getData();
    Optional<CallbackData> parsed = CallbackData.parse(raw);
    if (parsed.isEmpty()) {
        dispatchUnknown(ctx);
        return;
    }
    CallbackData data = parsed.get();
    Optional<TelegramScene> sceneOpt = sceneRegistry.findByKey(data.scene());
    if (sceneOpt.isEmpty()) {
        dispatchUnknown(ctx);
        return;
    }
    TelegramScene scene = sceneOpt.get();

    // handle :back — render parent via edit-in-place
    if ("back".equals(data.action())) {
        Optional<TelegramScene> parent = sceneRegistry.findByKey(scene.parentKey());
        if (parent.isPresent()) {
            SceneOutput output = parent.get().render(ctx);
            Message editTarget = ctx.callbackMessage().orElse(null);
            SceneOutput edit = output.editInPlace() ? output : output.asEdit();
            renderer.dispatch(edit, ctx.chatId(), editTarget);
            return;
        }
    }

    SceneOutput output = scene.handle(data, ctx);
    Message editTarget = ctx.callbackMessage().orElse(null);
    renderer.dispatch(output, ctx.chatId(), editTarget);
}
```

Also add helper to SceneOutput (in Task 2 structure this can be a new factory) — actually simpler: router handles parent render correctly without needing `asEdit`:

Simpler version:

```java
if ("back".equals(data.action())) {
    Optional<TelegramScene> parent = sceneRegistry.findByKey(scene.parentKey());
    if (parent.isPresent()) {
        SceneOutput parentOutput = parent.get().render(ctx);
        // force edit-in-place for back navigation
        SceneOutput editVersion = new SceneOutput(
            parentOutput.text(), parentOutput.inlineKeyboard(), null, true,
            parentOutput.parseMode(), null, null);
        renderer.dispatch(editVersion, ctx.chatId(), ctx.callbackMessage().orElse(null));
        return;
    }
}
```

- [ ] **Step 3: Add router test**

In `TelegramRouterTest.java`:

```java
@Test
void routesBackCallback_toParentSceneViaEdit() {
    Update update = callbackUpdate(100L, "qr_details:back");
    mockAuthorized(100L, 42L);
    TelegramScene detailsScene = mock(TelegramScene.class);
    TelegramScene listScene = mock(TelegramScene.class);
    when(detailsScene.parentKey()).thenReturn("qr_list");
    when(sceneRegistry.findByKey("qr_details")).thenReturn(Optional.of(detailsScene));
    when(sceneRegistry.findByKey("qr_list")).thenReturn(Optional.of(listScene));
    when(listScene.render(any())).thenReturn(SceneOutput.sendHtml("list", null));

    router.route(update);

    verify(listScene).render(any());
    verify(renderer).dispatch(any(SceneOutput.class), anyLong(), any());
}
```

Add `import static org.mockito.Mockito.mock;`.

- [ ] **Step 4: Run tests**

Run: `cd backend && ./gradlew test --tests "ru.car.service.message.telegram.router.TelegramRouterTest"`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add backend/src/main/java/ru/car/service/message/telegram/scene/TelegramScene.java \
        backend/src/main/java/ru/car/service/message/telegram/router/TelegramRouter.java \
        backend/src/test/java/ru/car/service/message/telegram/router/TelegramRouterTest.java
git commit -m "feat(tg-bot): router handles :back callback via parentKey + edit"
```

---

### Task 12: `HomeScene` (replaces `HomeMenuScene`)

**Files:**
- Create: `backend/src/main/java/ru/car/service/message/telegram/scene/impl/HomeScene.java`
- Create: `backend/src/test/java/ru/car/service/message/telegram/scene/impl/HomeSceneTest.java`
- Keep `HomeMenuScene.java` for now (will delete in Task 22 after all scenes wired).

- [ ] **Step 1: Write failing test**

Create `HomeSceneTest.java`:

```java
package ru.car.service.message.telegram.scene.impl;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;
import org.springframework.context.support.ReloadableResourceBundleMessageSource;
import org.telegram.telegrambots.meta.api.objects.replykeyboard.InlineKeyboardMarkup;
import ru.car.service.NotificationService;
import ru.car.service.QrService;
import ru.car.service.message.telegram.render.TelegramMessages;
import ru.car.service.message.telegram.router.CallbackData;
import ru.car.service.message.telegram.router.TelegramUpdateContext;
import ru.car.service.message.telegram.scene.SceneOutput;

import static org.assertj.core.api.Assertions.assertThat;
import static org.mockito.Mockito.when;

@ExtendWith(MockitoExtension.class)
class HomeSceneTest {

    @Mock NotificationService notificationService;
    @Mock QrService qrService;

    HomeScene scene;

    @BeforeEach
    void setUp() {
        scene = new HomeScene(notificationService, qrService, messages());
    }

    @Test
    void keyIsHome() {
        assertThat(scene.key()).isEqualTo("home");
    }

    @Test
    void render_withCounts_buildsFiveButtons() {
        when(notificationService.countUnreadByUserId(42L)).thenReturn(2);
        when(qrService.countByUserId(42L)).thenReturn(3);

        SceneOutput output = scene.render(new TelegramUpdateContext(100L, 42L, null, null));

        assertThat(output.text()).contains("Car ID");
        assertThat(output.parseMode()).isEqualTo("HTML");
        InlineKeyboardMarkup kb = output.inlineKeyboard();
        assertThat(kb).isNotNull();
        assertThat(kb.getKeyboard()).hasSize(5);
    }

    @Test
    void renderUnknown_hasHomeButton() {
        SceneOutput output = scene.renderUnknown(new TelegramUpdateContext(100L, 42L, null, null));
        assertThat(output.text()).contains("Неизвестная команда");
        assertThat(output.inlineKeyboard()).isNotNull();
    }

    @Test
    void handle_openCallback_rendersHome() {
        when(notificationService.countUnreadByUserId(42L)).thenReturn(0);
        when(qrService.countByUserId(42L)).thenReturn(0);

        SceneOutput output = scene.handle(
            new CallbackData("home", "open", java.util.List.of()),
            new TelegramUpdateContext(100L, 42L, null, null));

        assertThat(output.text()).contains("Car ID");
        assertThat(output.editInPlace()).isTrue();
    }

    private static TelegramMessages messages() {
        var src = new ReloadableResourceBundleMessageSource();
        src.setBasename("classpath:i18n/telegram");
        src.setDefaultEncoding("UTF-8");
        src.setFallbackToSystemLocale(false);
        return new TelegramMessages(src);
    }
}
```

- [ ] **Step 2: Run → fail** (compile error).

- [ ] **Step 3: Create HomeScene**

Create `HomeScene.java`:

```java
package ru.car.service.message.telegram.scene.impl;

import org.springframework.stereotype.Component;
import org.telegram.telegrambots.meta.api.objects.replykeyboard.InlineKeyboardMarkup;
import org.telegram.telegrambots.meta.api.objects.replykeyboard.buttons.InlineKeyboardButton;
import ru.car.service.NotificationService;
import ru.car.service.QrService;
import ru.car.service.message.telegram.render.TelegramMessages;
import ru.car.service.message.telegram.router.CallbackData;
import ru.car.service.message.telegram.router.TelegramUpdateContext;
import ru.car.service.message.telegram.scene.SceneOutput;
import ru.car.service.message.telegram.scene.TelegramScene;

import java.util.ArrayList;
import java.util.List;

@Component
public class HomeScene implements TelegramScene {

    public static final String KEY = "home";

    private final NotificationService notificationService;
    private final QrService qrService;
    private final TelegramMessages messages;

    public HomeScene(NotificationService notificationService, QrService qrService, TelegramMessages messages) {
        this.notificationService = notificationService;
        this.qrService = qrService;
        this.messages = messages;
    }

    @Override
    public String key() {
        return KEY;
    }

    @Override
    public SceneOutput render(TelegramUpdateContext ctx) {
        return SceneOutput.sendHtml(messages.get("tg.home.title"), buildKeyboard(ctx.userId()));
    }

    @Override
    public SceneOutput handle(CallbackData data, TelegramUpdateContext ctx) {
        // any callback in "home" scene just re-renders as edit-in-place
        return SceneOutput.editHtml(messages.get("tg.home.title"), buildKeyboard(ctx.userId()));
    }

    public SceneOutput renderUnknown(TelegramUpdateContext ctx) {
        InlineKeyboardMarkup kb = new InlineKeyboardMarkup();
        kb.setKeyboard(List.of(List.of(
            InlineKeyboardButton.builder()
                .text(messages.get("tg.unknown.btn.open_home"))
                .callbackData(new CallbackData("home", "open", List.of()).serialize())
                .build()
        )));
        return SceneOutput.sendHtml(messages.get("tg.home.unknown_command"), kb);
    }

    private InlineKeyboardMarkup buildKeyboard(Long userId) {
        int unread = notificationService.countUnreadByUserId(userId);
        int qrs = qrService.countByUserId(userId);

        String notifBtn = unread > 0
            ? messages.get("tg.home.btn.notifications", unread)
            : messages.get("tg.home.btn.notifications_zero");
        String qrsBtn = qrs > 0
            ? messages.get("tg.home.btn.qrs", qrs)
            : messages.get("tg.home.btn.qrs_zero");

        List<List<InlineKeyboardButton>> rows = new ArrayList<>();
        rows.add(row(notifBtn, "notif_list:open:all:1"));
        rows.add(row(qrsBtn, "qr_list:open"));
        rows.add(row(messages.get("tg.home.btn.temp_qr"), "temp_qr:create"));
        rows.add(row(messages.get("tg.home.btn.settings"), "settings:open"));
        rows.add(row(messages.get("tg.home.btn.profile"), "profile:open"));

        InlineKeyboardMarkup kb = new InlineKeyboardMarkup();
        kb.setKeyboard(rows);
        return kb;
    }

    private static List<InlineKeyboardButton> row(String text, String callback) {
        return List.of(InlineKeyboardButton.builder().text(text).callbackData(callback).build());
    }
}
```

- [ ] **Step 4: Run tests → pass**

Run: `cd backend && ./gradlew test --tests "ru.car.service.message.telegram.scene.impl.HomeSceneTest"`
Expected: PASS (4/4).

- [ ] **Step 5: Commit**

```bash
git add backend/src/main/java/ru/car/service/message/telegram/scene/impl/HomeScene.java \
        backend/src/test/java/ru/car/service/message/telegram/scene/impl/HomeSceneTest.java
git commit -m "feat(tg-bot): HomeScene with counters (replaces HomeMenuScene)"
```

---

### Task 13: `QrListScene` — rewrite

**Files:**
- Modify: `backend/src/main/java/ru/car/service/message/telegram/scene/impl/QrListScene.java`
- Modify: `backend/src/test/java/ru/car/service/message/telegram/scene/impl/QrListSceneTest.java`

- [ ] **Step 1: Rewrite test**

Replace content of `QrListSceneTest.java` with:

```java
package ru.car.service.message.telegram.scene.impl;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;
import org.springframework.context.support.ReloadableResourceBundleMessageSource;
import ru.car.enums.QrStatus;
import ru.car.model.Qr;
import ru.car.repository.QrRepository;
import ru.car.service.message.telegram.render.TelegramMessages;
import ru.car.service.message.telegram.router.CallbackData;
import ru.car.service.message.telegram.router.TelegramUpdateContext;
import ru.car.service.message.telegram.scene.SceneOutput;

import java.time.LocalDateTime;
import java.util.List;
import java.util.UUID;

import static org.assertj.core.api.Assertions.assertThat;
import static org.mockito.Mockito.when;

@ExtendWith(MockitoExtension.class)
class QrListSceneTest {

    @Mock QrRepository qrRepository;

    QrListScene scene;

    @BeforeEach
    void setUp() {
        scene = new QrListScene(qrRepository, messages());
    }

    @Test
    void keyIsQrList() {
        assertThat(scene.key()).isEqualTo("qr_list");
    }

    @Test
    void parentIsHome() {
        assertThat(scene.parentKey()).isEqualTo("home");
    }

    @Test
    void canHandleText_qrCodes() {
        assertThat(scene.canHandleText("QR-коды")).isTrue();
    }

    @Test
    void render_empty_returnsEmptyCardWithOzonButton() {
        when(qrRepository.findByUserId(42L)).thenReturn(List.of());

        SceneOutput output = scene.render(new TelegramUpdateContext(100L, 42L, null, null));

        assertThat(output.text()).contains("Мои метки");
        assertThat(output.parseMode()).isEqualTo("HTML");
        assertThat(output.inlineKeyboard()).isNotNull();
    }

    @Test
    void render_withQrs_groupsByStatus() {
        UUID id1 = UUID.randomUUID();
        Qr active = new Qr();
        active.setId(id1);
        active.setName("Audi Q5");
        active.setSeqNumber(145L);
        active.setStatus(QrStatus.ACTIVE);
        active.setActivateDate(LocalDateTime.of(2026, 3, 21, 10, 0));

        when(qrRepository.findByUserId(42L)).thenReturn(List.of(active));

        SceneOutput output = scene.render(new TelegramUpdateContext(100L, 42L, null, null));

        assertThat(output.text()).contains("Audi Q5").contains("145");
        assertThat(output.inlineKeyboard().getKeyboard()).hasSizeGreaterThanOrEqualTo(2); // 1 qr + back
    }

    @Test
    void handle_open_returnsEditInPlace() {
        when(qrRepository.findByUserId(42L)).thenReturn(List.of());

        SceneOutput output = scene.handle(
            new CallbackData("qr_list", "open", List.of()),
            new TelegramUpdateContext(100L, 42L, null, null));

        assertThat(output.editInPlace()).isTrue();
    }

    private static TelegramMessages messages() {
        var src = new ReloadableResourceBundleMessageSource();
        src.setBasename("classpath:i18n/telegram");
        src.setDefaultEncoding("UTF-8");
        src.setFallbackToSystemLocale(false);
        return new TelegramMessages(src);
    }
}
```

- [ ] **Step 2: Rewrite QrListScene**

Replace `QrListScene.java`:

```java
package ru.car.service.message.telegram.scene.impl;

import lombok.extern.slf4j.Slf4j;
import org.apache.commons.collections4.CollectionUtils;
import org.springframework.stereotype.Component;
import org.telegram.telegrambots.meta.api.objects.replykeyboard.InlineKeyboardMarkup;
import org.telegram.telegrambots.meta.api.objects.replykeyboard.buttons.InlineKeyboardButton;
import ru.car.enums.QrStatus;
import ru.car.model.Qr;
import ru.car.repository.QrRepository;
import ru.car.service.message.telegram.render.TelegramMessages;
import ru.car.service.message.telegram.router.CallbackData;
import ru.car.service.message.telegram.router.TelegramUpdateContext;
import ru.car.service.message.telegram.scene.SceneOutput;
import ru.car.service.message.telegram.scene.TelegramScene;
import ru.car.util.MessageUtils;

import java.time.format.DateTimeFormatter;
import java.util.ArrayList;
import java.util.List;

@Slf4j
@Component
public class QrListScene implements TelegramScene {

    public static final String KEY = "qr_list";
    public static final String ACTION_OPEN = "open";

    private static final DateTimeFormatter DATE_FMT = DateTimeFormatter.ofPattern("d MMMM yyyy",
        new java.util.Locale("ru", "RU"));
    private static final DateTimeFormatter TIME_FMT = DateTimeFormatter.ofPattern("HH:mm");

    private final QrRepository qrRepository;
    private final TelegramMessages messages;

    public QrListScene(QrRepository qrRepository, TelegramMessages messages) {
        this.qrRepository = qrRepository;
        this.messages = messages;
    }

    @Override
    public String key() {
        return KEY;
    }

    @Override
    public boolean canHandleText(String text) {
        return "QR-коды".equals(text);
    }

    @Override
    public SceneOutput render(TelegramUpdateContext ctx) {
        return build(ctx.userId(), false);
    }

    @Override
    public SceneOutput handle(CallbackData data, TelegramUpdateContext ctx) {
        if (!ACTION_OPEN.equals(data.action())) {
            return SceneOutput.noop();
        }
        return build(ctx.userId(), true);
    }

    private SceneOutput build(Long userId, boolean edit) {
        List<Qr> qrs = qrRepository.findByUserId(userId);
        if (CollectionUtils.isEmpty(qrs)) {
            return buildEmpty(edit);
        }
        StringBuilder body = new StringBuilder(messages.get("tg.qr.list.title"));
        List<List<InlineKeyboardButton>> rows = new ArrayList<>();

        for (Qr qr : qrs) {
            if (QrStatus.DELETED.equals(qr.getStatus())) continue;
            body.append("\n\n").append(formatCard(qr));
            rows.add(row(shortName(qr), "qr_details:open:" + qr.getId()));
        }
        rows.add(List.of(
            InlineKeyboardButton.builder().text(messages.get("tg.common.back")).callbackData("qr_list:back").build()
        ));

        InlineKeyboardMarkup kb = new InlineKeyboardMarkup();
        kb.setKeyboard(rows);
        return edit ? SceneOutput.editHtml(body.toString(), kb) : SceneOutput.sendHtml(body.toString(), kb);
    }

    private SceneOutput buildEmpty(boolean edit) {
        InlineKeyboardMarkup kb = new InlineKeyboardMarkup();
        List<List<InlineKeyboardButton>> rows = new ArrayList<>();
        rows.add(row(messages.get("tg.home.btn.temp_qr"), "temp_qr:create"));
        rows.add(List.of(
            InlineKeyboardButton.builder()
                .text(messages.get("tg.qr.list.btn.ozon"))
                .url("https://ozon.ru/t/gXqGlgj")
                .build()
        ));
        rows.add(row(messages.get("tg.common.back"), "qr_list:back"));
        kb.setKeyboard(rows);
        String body = messages.get("tg.qr.list.empty_v2");
        return edit ? SceneOutput.editHtml(body, kb) : SceneOutput.sendHtml(body, kb);
    }

    private String formatCard(Qr qr) {
        String name = MessageUtils.escapeHtml(qr.getName());
        String emoji = statusEmoji(qr.getStatus());
        return switch (qr.getStatus()) {
            case TEMPORARY -> messages.get("tg.qr.list.card.temp", TIME_FMT.format(qr.getCreatedDate().plusHours(1)));
            case DELETED -> messages.get("tg.qr.list.card.disabled", name, qr.getSeqNumber());
            default -> messages.get("tg.qr.list.card.active",
                emoji,
                name != null ? name : "",
                qr.getSeqNumber(),
                qr.getActivateDate() != null ? DATE_FMT.format(qr.getActivateDate()) : "-",
                0);  // notifications count shown as dash in 2.2; full count in 2.5
        };
    }

    private String shortName(Qr qr) {
        String emoji = statusEmoji(qr.getStatus());
        String name = qr.getName() != null ? qr.getName() : "Временная";
        return emoji + " " + name;
    }

    private String statusEmoji(QrStatus s) {
        return switch (s) {
            case ACTIVE -> "🟢";
            case DELETED -> "⏸";
            case TEMPORARY -> "🕐";
            case NEW -> "⚪";
        };
    }

    private static List<InlineKeyboardButton> row(String text, String callback) {
        return List.of(InlineKeyboardButton.builder().text(text).callbackData(callback).build());
    }
}
```

Note: scene no longer uses `TelegramTransport` / PDF-generation. PDF is removed; PNG lives in `QrDetailsScene` (Task 14).

- [ ] **Step 3: Run tests → pass**

Run: `cd backend && ./gradlew test --tests "ru.car.service.message.telegram.scene.impl.QrListSceneTest"`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add backend/src/main/java/ru/car/service/message/telegram/scene/impl/QrListScene.java \
        backend/src/test/java/ru/car/service/message/telegram/scene/impl/QrListSceneTest.java
git commit -m "feat(tg-bot): QrListScene v2 — HTML cards, status emojis, details link"
```

---

### Task 14: `QrDetailsScene`

**Files:**
- Create: `backend/src/main/java/ru/car/service/message/telegram/scene/impl/QrDetailsScene.java`
- Create: `backend/src/test/java/ru/car/service/message/telegram/scene/impl/QrDetailsSceneTest.java`

- [ ] **Step 1: Write failing test**

```java
package ru.car.service.message.telegram.scene.impl;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;
import org.springframework.context.support.ReloadableResourceBundleMessageSource;
import ru.car.enums.QrStatus;
import ru.car.model.Qr;
import ru.car.service.NotificationService;
import ru.car.service.QrService;
import ru.car.service.message.telegram.render.TelegramMessages;
import ru.car.service.message.telegram.router.CallbackData;
import ru.car.service.message.telegram.router.TelegramUpdateContext;
import ru.car.service.message.telegram.scene.SceneOutput;

import java.time.LocalDateTime;
import java.util.List;
import java.util.UUID;

import static org.assertj.core.api.Assertions.assertThat;
import static org.mockito.Mockito.verify;
import static org.mockito.Mockito.when;

@ExtendWith(MockitoExtension.class)
class QrDetailsSceneTest {

    @Mock QrService qrService;
    @Mock NotificationService notificationService;

    QrDetailsScene scene;

    @BeforeEach
    void setUp() {
        scene = new QrDetailsScene(qrService, notificationService, messages());
        org.springframework.test.util.ReflectionTestUtils.setField(scene, "serverUrl", "https://car-id.ru");
    }

    @Test
    void keyIsQrDetails() {
        assertThat(scene.key()).isEqualTo("qr_details");
    }

    @Test
    void parentIsQrList() {
        assertThat(scene.parentKey()).isEqualTo("qr_list");
    }

    @Test
    void handle_open_rendersDetails() {
        UUID id = UUID.randomUUID();
        Qr qr = new Qr();
        qr.setId(id);
        qr.setName("Audi Q5");
        qr.setSeqNumber(145L);
        qr.setStatus(QrStatus.ACTIVE);
        qr.setActivateDate(LocalDateTime.of(2026, 3, 21, 10, 0));
        when(qrService.findByIdOrThrowNotFound(id)).thenReturn(qr);
        // notifications count: if notification service doesn't have per-qr count, return 0

        SceneOutput output = scene.handle(
            new CallbackData("qr_details", "open", List.of(id.toString())),
            new TelegramUpdateContext(100L, 42L, null, null));

        assertThat(output.text()).contains("Audi Q5").contains("145").contains("Активна");
        assertThat(output.editInPlace()).isTrue();
        assertThat(output.inlineKeyboard()).isNotNull();
    }

    @Test
    void handle_show_returnsPhotoOutput() {
        UUID id = UUID.randomUUID();
        Qr qr = new Qr();
        qr.setId(id);
        qr.setName("Audi Q5");
        qr.setSeqNumber(145L);
        when(qrService.findByIdOrThrowNotFound(id)).thenReturn(qr);

        SceneOutput output = scene.handle(
            new CallbackData("qr_details", "show", List.of(id.toString())),
            new TelegramUpdateContext(100L, 42L, null, null));

        assertThat(output.photo()).isNotNull();
        assertThat(output.caption()).contains("Audi Q5");
    }

    @Test
    void handle_disable_returnsConfirmDialog() {
        UUID id = UUID.randomUUID();
        SceneOutput output = scene.handle(
            new CallbackData("qr_details", "disable", List.of(id.toString())),
            new TelegramUpdateContext(100L, 42L, null, null));

        assertThat(output.text()).contains("Отключить метку");
        assertThat(output.editInPlace()).isTrue();
    }

    @Test
    void handle_disableConfirm_callsServiceAndRerenders() {
        UUID id = UUID.randomUUID();
        Qr qr = new Qr();
        qr.setId(id);
        qr.setName("Audi Q5");
        qr.setStatus(QrStatus.DELETED);
        when(qrService.findByIdOrThrowNotFound(id)).thenReturn(qr);

        SceneOutput output = scene.handle(
            new CallbackData("qr_details", "disable_confirm", List.of(id.toString())),
            new TelegramUpdateContext(100L, 42L, null, null));

        verify(qrService).disable(id);
        assertThat(output.text()).contains("Отключена");
    }

    private static TelegramMessages messages() {
        var src = new ReloadableResourceBundleMessageSource();
        src.setBasename("classpath:i18n/telegram");
        src.setDefaultEncoding("UTF-8");
        src.setFallbackToSystemLocale(false);
        return new TelegramMessages(src);
    }
}
```

- [ ] **Step 2: Run → fail**

Run: `cd backend && ./gradlew test --tests "ru.car.service.message.telegram.scene.impl.QrDetailsSceneTest"`
Expected: FAIL (class doesn't exist).

- [ ] **Step 3: Create QrDetailsScene**

```java
package ru.car.service.message.telegram.scene.impl;

import lombok.extern.slf4j.Slf4j;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.core.io.Resource;
import org.springframework.stereotype.Component;
import org.telegram.telegrambots.meta.api.objects.InputFile;
import org.telegram.telegrambots.meta.api.objects.replykeyboard.InlineKeyboardMarkup;
import org.telegram.telegrambots.meta.api.objects.replykeyboard.buttons.InlineKeyboardButton;
import ru.car.enums.QrStatus;
import ru.car.model.Qr;
import ru.car.service.NotificationService;
import ru.car.service.QrService;
import ru.car.service.message.telegram.render.TelegramMessages;
import ru.car.service.message.telegram.router.CallbackData;
import ru.car.service.message.telegram.router.TelegramUpdateContext;
import ru.car.service.message.telegram.scene.SceneOutput;
import ru.car.service.message.telegram.scene.TelegramScene;
import ru.car.util.MessageUtils;
import ru.car.util.QrUtils;

import java.io.IOException;
import java.time.format.DateTimeFormatter;
import java.util.List;
import java.util.UUID;

@Slf4j
@Component
public class QrDetailsScene implements TelegramScene {

    public static final String KEY = "qr_details";
    public static final String ACTION_OPEN = "open";
    public static final String ACTION_SHOW = "show";
    public static final String ACTION_DISABLE = "disable";
    public static final String ACTION_DISABLE_CONFIRM = "disable_confirm";

    private static final DateTimeFormatter DATE_FMT = DateTimeFormatter.ofPattern("d MMMM yyyy",
        new java.util.Locale("ru", "RU"));

    private final QrService qrService;
    private final NotificationService notificationService;
    private final TelegramMessages messages;

    @Value("${swagger.server.url}")
    private String serverUrl;

    public QrDetailsScene(QrService qrService, NotificationService notificationService, TelegramMessages messages) {
        this.qrService = qrService;
        this.notificationService = notificationService;
        this.messages = messages;
    }

    @Override
    public String key() {
        return KEY;
    }

    @Override
    public String parentKey() {
        return "qr_list";
    }

    @Override
    public SceneOutput handle(CallbackData data, TelegramUpdateContext ctx) {
        if (data.args().isEmpty() || !MessageUtils.isUUID(data.args().get(0))) {
            return SceneOutput.noop();
        }
        UUID id = UUID.fromString(data.args().get(0));

        return switch (data.action()) {
            case ACTION_OPEN -> renderDetails(id, true);
            case ACTION_SHOW -> renderPhoto(id);
            case ACTION_DISABLE -> renderDisableConfirm(id);
            case ACTION_DISABLE_CONFIRM -> {
                try {
                    qrService.disable(id);
                } catch (Exception e) {
                    log.warn("disable failed for {}", id, e);
                }
                yield renderDetails(id, true);
            }
            default -> SceneOutput.noop();
        };
    }

    private SceneOutput renderDetails(UUID id, boolean edit) {
        Qr qr = qrService.findByIdOrThrowNotFound(id);
        String emoji = statusEmoji(qr.getStatus());
        String name = MessageUtils.escapeHtml(qr.getName() != null ? qr.getName() : "Метка");
        String statusText = statusText(qr.getStatus());
        String activated = qr.getActivateDate() != null ? DATE_FMT.format(qr.getActivateDate()) : "-";
        int totalNotif = 0;
        int unreadNotif = 0;
        // count notifications: use existing services if method available, else 0

        String title = messages.get("tg.qr.details.title", emoji, name);
        String fields = messages.get("tg.qr.details.fields",
            qr.getSeqNumber(),
            emoji + " " + statusText,
            activated,
            totalNotif,
            unreadNotif);
        String body = title + "\n\n" + fields;

        InlineKeyboardMarkup kb = new InlineKeyboardMarkup();
        kb.setKeyboard(List.of(
            row(messages.get("tg.qr.details.btn.show"), "qr_details:show:" + id),
            row(messages.get("tg.qr.details.btn.history"), "notif_list:open:all:1:qr:" + id),
            row(messages.get("tg.qr.details.btn.disable"), "qr_details:disable:" + id),
            List.of(
                btn(messages.get("tg.qr.details.btn.back_to_list"), "qr_details:back"),
                btn(messages.get("tg.common.home"), "home:open")
            )
        ));
        return edit ? SceneOutput.editHtml(body, kb) : SceneOutput.sendHtml(body, kb);
    }

    private SceneOutput renderPhoto(UUID id) {
        Qr qr = qrService.findByIdOrThrowNotFound(id);
        String url = QrUtils.getQrUrlById(serverUrl, id);
        try {
            Resource pngResource = QrUtils.generateQRCodeImage(url);
            InputFile input = new InputFile(pngResource.getInputStream(), "qr-" + id + ".png");
            String name = MessageUtils.escapeHtml(qr.getName() != null ? qr.getName() : "Метка");
            String caption = "<b>" + name + "</b> · №" + qr.getSeqNumber();
            return SceneOutput.photo(input, caption);
        } catch (IOException e) {
            log.error("Failed to generate QR image for {}", id, e);
            return SceneOutput.noop();
        }
    }

    private SceneOutput renderDisableConfirm(UUID id) {
        InlineKeyboardMarkup kb = new InlineKeyboardMarkup();
        kb.setKeyboard(List.of(
            row(messages.get("tg.common.confirm.yes"), "qr_details:disable_confirm:" + id),
            row(messages.get("tg.common.confirm.cancel"), "qr_details:open:" + id)
        ));
        return SceneOutput.editHtml(messages.get("tg.qr.details.confirm.disable"), kb);
    }

    private String statusText(QrStatus s) {
        return switch (s) {
            case ACTIVE -> messages.get("tg.qr.details.status.active");
            case DELETED -> messages.get("tg.qr.details.status.disabled");
            case TEMPORARY -> messages.get("tg.qr.details.status.temp");
            case NEW -> messages.get("tg.qr.details.status.new");
        };
    }

    private String statusEmoji(QrStatus s) {
        return switch (s) {
            case ACTIVE -> "🟢";
            case DELETED -> "⏸";
            case TEMPORARY -> "🕐";
            case NEW -> "⚪";
        };
    }

    private static List<InlineKeyboardButton> row(String text, String callback) {
        return List.of(btn(text, callback));
    }

    private static InlineKeyboardButton btn(String text, String callback) {
        return InlineKeyboardButton.builder().text(text).callbackData(callback).build();
    }
}
```

- [ ] **Step 4: Run → pass**

Run: `cd backend && ./gradlew test --tests "ru.car.service.message.telegram.scene.impl.QrDetailsSceneTest"`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add backend/src/main/java/ru/car/service/message/telegram/scene/impl/QrDetailsScene.java \
        backend/src/test/java/ru/car/service/message/telegram/scene/impl/QrDetailsSceneTest.java
git commit -m "feat(tg-bot): QrDetailsScene — details + PNG + disable confirm"
```

---

### Task 15: `NotificationListScene`

**Files:**
- Create: `backend/src/main/java/ru/car/service/message/telegram/scene/impl/NotificationListScene.java`
- Create: `backend/src/test/java/ru/car/service/message/telegram/scene/impl/NotificationListSceneTest.java`

- [ ] **Step 1: Write test skeleton**

```java
package ru.car.service.message.telegram.scene.impl;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;
import org.springframework.context.support.ReloadableResourceBundleMessageSource;
import ru.car.dto.PageParam;
import ru.car.enums.NotificationStatus;
import ru.car.model.Notification;
import ru.car.service.NotificationService;
import ru.car.service.message.TextMessage;
import ru.car.service.message.telegram.render.TelegramMessages;
import ru.car.service.message.telegram.router.CallbackData;
import ru.car.service.message.telegram.router.TelegramUpdateContext;
import ru.car.service.message.telegram.scene.SceneOutput;
import ru.car.service.message.telegram.scene.impl.NotificationMarkReadScene;

import java.time.LocalDateTime;
import java.util.List;
import java.util.UUID;

import static org.assertj.core.api.Assertions.assertThat;
import static org.mockito.ArgumentMatchers.any;
import static org.mockito.ArgumentMatchers.eq;
import static org.mockito.Mockito.when;

@ExtendWith(MockitoExtension.class)
class NotificationListSceneTest {

    @Mock NotificationService notificationService;
    @Mock NotificationMarkReadScene markReadScene;

    NotificationListScene scene;

    @BeforeEach
    void setUp() {
        scene = new NotificationListScene(notificationService, markReadScene, messages());
    }

    @Test
    void keyIsNotifList() {
        assertThat(scene.key()).isEqualTo("notif_list");
    }

    @Test
    void handle_openAllPage1_rendersList() {
        when(notificationService.findForBot(eq(42L), eq("all"), eq(null), any(PageParam.class)))
            .thenReturn(List.of());

        SceneOutput output = scene.handle(
            new CallbackData("notif_list", "open", List.of("all", "1")),
            new TelegramUpdateContext(100L, 42L, null, null));

        assertThat(output.text()).contains("Уведомления");
        assertThat(output.editInPlace()).isTrue();
    }

    @Test
    void handle_openWithQrFilter_passesQrIdToService() {
        UUID qr = UUID.randomUUID();
        when(notificationService.findForBot(eq(42L), eq("all"), eq(qr), any(PageParam.class)))
            .thenReturn(List.of());

        SceneOutput output = scene.handle(
            new CallbackData("notif_list", "open", List.of("all", "1", "qr", qr.toString())),
            new TelegramUpdateContext(100L, 42L, null, null));

        assertThat(output.editInPlace()).isTrue();
    }

    @Test
    void handle_view_delegatesToMarkReadScene() {
        UUID notifId = UUID.randomUUID();
        Notification n = new Notification();
        n.setId(notifId);
        n.setText("test");
        n.setStatus(NotificationStatus.UNREAD);
        n.setCreatedDate(LocalDateTime.now());
        when(notificationService.findByIdOrThrowNotFound(notifId)).thenReturn(n);
        when(markReadScene.renderNotification(any(TextMessage.class)))
            .thenReturn(SceneOutput.sendWithInline("body", null));

        SceneOutput output = scene.handle(
            new CallbackData("notif_list", "view", List.of(notifId.toString())),
            new TelegramUpdateContext(100L, 42L, null, null));

        assertThat(output.text()).isEqualTo("body");
    }

    private static TelegramMessages messages() {
        var src = new ReloadableResourceBundleMessageSource();
        src.setBasename("classpath:i18n/telegram");
        src.setDefaultEncoding("UTF-8");
        src.setFallbackToSystemLocale(false);
        return new TelegramMessages(src);
    }
}
```

- [ ] **Step 2: Run → fail**

- [ ] **Step 3: Create scene**

```java
package ru.car.service.message.telegram.scene.impl;

import lombok.extern.slf4j.Slf4j;
import org.springframework.stereotype.Component;
import org.telegram.telegrambots.meta.api.objects.replykeyboard.InlineKeyboardMarkup;
import org.telegram.telegrambots.meta.api.objects.replykeyboard.buttons.InlineKeyboardButton;
import ru.car.dto.PageParam;
import ru.car.enums.NotificationStatus;
import ru.car.model.Notification;
import ru.car.model.NotificationSetting;
import ru.car.service.NotificationService;
import ru.car.service.message.TextMessage;
import ru.car.service.message.telegram.render.TelegramMessages;
import ru.car.service.message.telegram.router.CallbackData;
import ru.car.service.message.telegram.router.TelegramUpdateContext;
import ru.car.service.message.telegram.scene.SceneOutput;
import ru.car.service.message.telegram.scene.TelegramScene;
import ru.car.util.MessageUtils;

import java.time.LocalDate;
import java.time.format.DateTimeFormatter;
import java.util.ArrayList;
import java.util.List;
import java.util.UUID;

@Slf4j
@Component
public class NotificationListScene implements TelegramScene {

    public static final String KEY = "notif_list";
    public static final String ACTION_OPEN = "open";
    public static final String ACTION_VIEW = "view";
    private static final int PAGE_SIZE = 5;

    private static final DateTimeFormatter TIME = DateTimeFormatter.ofPattern("HH:mm");
    private static final DateTimeFormatter DATE = DateTimeFormatter.ofPattern("dd.MM");

    private final NotificationService notificationService;
    private final NotificationMarkReadScene markReadScene;
    private final TelegramMessages messages;

    public NotificationListScene(NotificationService notificationService,
                                 NotificationMarkReadScene markReadScene,
                                 TelegramMessages messages) {
        this.notificationService = notificationService;
        this.markReadScene = markReadScene;
        this.messages = messages;
    }

    @Override
    public String key() {
        return KEY;
    }

    @Override
    public SceneOutput handle(CallbackData data, TelegramUpdateContext ctx) {
        return switch (data.action()) {
            case ACTION_OPEN -> renderList(data.args(), ctx);
            case ACTION_VIEW -> viewNotification(data.args(), ctx);
            default -> SceneOutput.noop();
        };
    }

    private SceneOutput renderList(List<String> args, TelegramUpdateContext ctx) {
        String tab = args.size() > 0 ? args.get(0) : "all";
        int page = args.size() > 1 ? parseIntSafe(args.get(1), 1) : 1;
        UUID qrFilter = null;
        if (args.size() >= 4 && "qr".equals(args.get(2)) && MessageUtils.isUUID(args.get(3))) {
            qrFilter = UUID.fromString(args.get(3));
        }

        PageParam pageParam = PageParam.builder().page(page - 1).size(PAGE_SIZE).build();
        List<Notification> notifications = notificationService.findForBot(ctx.userId(), tab, qrFilter, pageParam);

        if (notifications.isEmpty() && page == 1) {
            InlineKeyboardMarkup kb = new InlineKeyboardMarkup();
            kb.setKeyboard(List.of(List.of(
                btn(messages.get("tg.common.back"), KEY + ":back"),
                btn(messages.get("tg.common.home"), "home:open")
            )));
            return SceneOutput.editHtml(messages.get("tg.notif.list.empty"), kb);
        }

        StringBuilder body = new StringBuilder(messages.get("tg.notif.list.title")).append("\n\n");
        body.append("all".equals(tab) ? messages.get("tg.notif.list.tabs_all") : messages.get("tg.notif.list.tabs_unread"));
        body.append("\n\n");
        for (Notification n : notifications) {
            body.append(formatItem(n)).append("\n\n");
        }

        List<List<InlineKeyboardButton>> rows = new ArrayList<>();
        for (Notification n : notifications) {
            rows.add(List.of(btn(formatButton(n), KEY + ":view:" + n.getId())));
        }
        String tailFilter = qrFilter != null ? ":qr:" + qrFilter : "";
        rows.add(List.of(
            btn(messages.get("tg.notif.list.btn.tab_all"), KEY + ":open:all:1" + tailFilter),
            btn(messages.get("tg.notif.list.btn.tab_unread"), KEY + ":open:unread:1" + tailFilter)
        ));
        if (notifications.size() == PAGE_SIZE || page > 1) {
            List<InlineKeyboardButton> pagination = new ArrayList<>();
            if (page > 1) {
                pagination.add(btn("‹", KEY + ":open:" + tab + ":" + (page - 1) + tailFilter));
            }
            pagination.add(btn(messages.get("tg.notif.list.pagination", page, "?"), "noop:noop"));
            if (notifications.size() == PAGE_SIZE) {
                pagination.add(btn("›", KEY + ":open:" + tab + ":" + (page + 1) + tailFilter));
            }
            rows.add(pagination);
        }
        rows.add(List.of(
            btn(messages.get("tg.common.back"), KEY + ":back"),
            btn(messages.get("tg.common.home"), "home:open")
        ));

        InlineKeyboardMarkup kb = new InlineKeyboardMarkup();
        kb.setKeyboard(rows);
        return SceneOutput.editHtml(body.toString(), kb);
    }

    private SceneOutput viewNotification(List<String> args, TelegramUpdateContext ctx) {
        if (args.isEmpty() || !MessageUtils.isUUID(args.get(0))) {
            return SceneOutput.noop();
        }
        UUID id = UUID.fromString(args.get(0));
        Notification n = notificationService.findByIdOrThrowNotFound(id);
        NotificationSetting setting = new NotificationSetting();
        setting.setTelegramDialogId(ctx.chatId());
        TextMessage msg = TextMessage.builder()
            .setting(setting)
            .notificationId(n.getId())
            .text(n.getText() != null ? n.getText() : "")
            .build();
        return markReadScene.renderNotification(msg);
    }

    private String formatItem(Notification n) {
        String emoji = NotificationStatus.UNREAD.equals(n.getStatus()) ? "🔴" : "⚪";
        String when = formatWhen(n.getCreatedDate());
        String qrName = "метка";  // 2.2 keeps it generic; detailed in 2.5
        String text = MessageUtils.escapeHtml(n.getText() != null ? n.getText() : "");
        return emoji + " " + when + " · <b>" + qrName + "</b>\n   " + text;
    }

    private String formatButton(Notification n) {
        String emoji = NotificationStatus.UNREAD.equals(n.getStatus()) ? "🔴" : "⚪";
        return emoji + " " + formatWhen(n.getCreatedDate());
    }

    private String formatWhen(java.time.LocalDateTime dt) {
        if (dt == null) return "";
        LocalDate today = LocalDate.now();
        LocalDate d = dt.toLocalDate();
        if (d.equals(today)) return TIME.format(dt);
        if (d.equals(today.minusDays(1))) return "вчера, " + TIME.format(dt);
        return DATE.format(dt);
    }

    private int parseIntSafe(String s, int fallback) {
        try { return Integer.parseInt(s); } catch (NumberFormatException e) { return fallback; }
    }

    private static InlineKeyboardButton btn(String text, String callback) {
        return InlineKeyboardButton.builder().text(text).callbackData(callback).build();
    }
}
```

- [ ] **Step 4: Run → pass**

Run: `cd backend && ./gradlew test --tests "ru.car.service.message.telegram.scene.impl.NotificationListSceneTest"`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add backend/src/main/java/ru/car/service/message/telegram/scene/impl/NotificationListScene.java \
        backend/src/test/java/ru/car/service/message/telegram/scene/impl/NotificationListSceneTest.java
git commit -m "feat(tg-bot): NotificationListScene — tabs + pagination + qr-filter"
```

---

### Task 16: `NotificationSettingsScene`

**Files:**
- Create: `backend/src/main/java/ru/car/service/message/telegram/scene/impl/NotificationSettingsScene.java`
- Create: `backend/src/test/java/ru/car/service/message/telegram/scene/impl/NotificationSettingsSceneTest.java`

- [ ] **Step 1: Write test**

```java
package ru.car.service.message.telegram.scene.impl;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;
import org.springframework.context.support.ReloadableResourceBundleMessageSource;
import ru.car.model.NotificationSetting;
import ru.car.service.NotificationSettingService;
import ru.car.repository.NotificationSettingRepository;
import ru.car.service.message.telegram.render.TelegramMessages;
import ru.car.service.message.telegram.router.CallbackData;
import ru.car.service.message.telegram.router.TelegramUpdateContext;
import ru.car.service.message.telegram.scene.SceneOutput;

import java.util.List;

import static org.assertj.core.api.Assertions.assertThat;
import static org.mockito.Mockito.verify;
import static org.mockito.Mockito.when;

@ExtendWith(MockitoExtension.class)
class NotificationSettingsSceneTest {

    @Mock NotificationSettingService settingService;
    @Mock NotificationSettingRepository settingRepository;

    NotificationSettingsScene scene;

    @BeforeEach
    void setUp() {
        scene = new NotificationSettingsScene(settingService, settingRepository, messages());
    }

    @Test
    void keyIsSettings() {
        assertThat(scene.key()).isEqualTo("settings");
    }

    @Test
    void handle_open_rendersWithCurrentState() {
        NotificationSetting setting = NotificationSetting.builder()
            .pushEnabled(true).callEnabled(false).telegramEnabled(true).build();
        when(settingRepository.findByUserId(42L)).thenReturn(setting);

        SceneOutput output = scene.handle(
            new CallbackData("settings", "open", List.of()),
            new TelegramUpdateContext(100L, 42L, null, null));

        assertThat(output.text()).contains("Настройки уведомлений");
        assertThat(output.editInPlace()).isTrue();
    }

    @Test
    void handle_toggle_delegatesAndRerenders() {
        NotificationSetting toggled = NotificationSetting.builder()
            .pushEnabled(false).callEnabled(false).telegramEnabled(true).build();
        when(settingService.toggleChannel(42L, "push")).thenReturn(toggled);
        when(settingRepository.findByUserId(42L)).thenReturn(toggled);

        SceneOutput output = scene.handle(
            new CallbackData("settings", "toggle", List.of("push")),
            new TelegramUpdateContext(100L, 42L, null, null));

        verify(settingService).toggleChannel(42L, "push");
        assertThat(output.editInPlace()).isTrue();
    }

    private static TelegramMessages messages() {
        var src = new ReloadableResourceBundleMessageSource();
        src.setBasename("classpath:i18n/telegram");
        src.setDefaultEncoding("UTF-8");
        src.setFallbackToSystemLocale(false);
        return new TelegramMessages(src);
    }
}
```

- [ ] **Step 2: Create scene**

```java
package ru.car.service.message.telegram.scene.impl;

import org.springframework.stereotype.Component;
import org.telegram.telegrambots.meta.api.objects.replykeyboard.InlineKeyboardMarkup;
import org.telegram.telegrambots.meta.api.objects.replykeyboard.buttons.InlineKeyboardButton;
import ru.car.model.NotificationSetting;
import ru.car.repository.NotificationSettingRepository;
import ru.car.service.NotificationSettingService;
import ru.car.service.message.telegram.render.TelegramMessages;
import ru.car.service.message.telegram.router.CallbackData;
import ru.car.service.message.telegram.router.TelegramUpdateContext;
import ru.car.service.message.telegram.scene.SceneOutput;
import ru.car.service.message.telegram.scene.TelegramScene;

import java.util.List;

@Component
public class NotificationSettingsScene implements TelegramScene {

    public static final String KEY = "settings";
    public static final String ACTION_OPEN = "open";
    public static final String ACTION_TOGGLE = "toggle";

    private final NotificationSettingService service;
    private final NotificationSettingRepository repository;
    private final TelegramMessages messages;

    public NotificationSettingsScene(NotificationSettingService service,
                                     NotificationSettingRepository repository,
                                     TelegramMessages messages) {
        this.service = service;
        this.repository = repository;
        this.messages = messages;
    }

    @Override
    public String key() {
        return KEY;
    }

    @Override
    public SceneOutput handle(CallbackData data, TelegramUpdateContext ctx) {
        return switch (data.action()) {
            case ACTION_OPEN -> render(ctx.userId());
            case ACTION_TOGGLE -> {
                if (data.args().isEmpty()) yield SceneOutput.noop();
                service.toggleChannel(ctx.userId(), data.args().get(0));
                yield render(ctx.userId());
            }
            default -> SceneOutput.noop();
        };
    }

    private SceneOutput render(Long userId) {
        NotificationSetting s = repository.findByUserId(userId);
        String body = messages.get("tg.settings.title") + "\n\n" +
            messages.get("tg.settings.body",
                formatState(s.getPushEnabled()),
                formatState(s.getCallEnabled()),
                formatState(s.getTelegramEnabled()));

        InlineKeyboardMarkup kb = new InlineKeyboardMarkup();
        kb.setKeyboard(List.of(
            List.of(btn(pushLabel(s.getPushEnabled()), "settings:toggle:push")),
            List.of(btn(callLabel(s.getCallEnabled()), "settings:toggle:call")),
            List.of(btn(telegramLabel(s.getTelegramEnabled()), "settings:toggle:telegram")),
            List.of(
                btn(messages.get("tg.common.back"), "settings:back"),
                btn(messages.get("tg.common.home"), "home:open")
            )
        ));
        return SceneOutput.editHtml(body, kb);
    }

    private String formatState(Boolean on) {
        return Boolean.TRUE.equals(on) ? messages.get("tg.settings.on") : messages.get("tg.settings.off");
    }

    private String pushLabel(Boolean on) {
        return Boolean.TRUE.equals(on) ? messages.get("tg.settings.btn.toggle_push_on") : messages.get("tg.settings.btn.toggle_push_off");
    }

    private String callLabel(Boolean on) {
        return Boolean.TRUE.equals(on) ? messages.get("tg.settings.btn.toggle_call_on") : messages.get("tg.settings.btn.toggle_call_off");
    }

    private String telegramLabel(Boolean on) {
        return Boolean.TRUE.equals(on) ? messages.get("tg.settings.btn.toggle_telegram_on") : messages.get("tg.settings.btn.toggle_telegram_off");
    }

    private static InlineKeyboardButton btn(String text, String callback) {
        return InlineKeyboardButton.builder().text(text).callbackData(callback).build();
    }
}
```

- [ ] **Step 3: Run → pass**

- [ ] **Step 4: Commit**

```bash
git add backend/src/main/java/ru/car/service/message/telegram/scene/impl/NotificationSettingsScene.java \
        backend/src/test/java/ru/car/service/message/telegram/scene/impl/NotificationSettingsSceneTest.java
git commit -m "feat(tg-bot): NotificationSettingsScene — 3 channel toggles"
```

---

### Task 17: `ProfileScene`

**Files:**
- Create: `backend/src/main/java/ru/car/service/message/telegram/scene/impl/ProfileScene.java`
- Create: `backend/src/test/java/ru/car/service/message/telegram/scene/impl/ProfileSceneTest.java`

- [ ] **Step 1: Write test**

```java
package ru.car.service.message.telegram.scene.impl;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;
import org.springframework.context.support.ReloadableResourceBundleMessageSource;
import ru.car.enums.Role;
import ru.car.model.User;
import ru.car.repository.NotificationSettingRepository;
import ru.car.service.UserService;
import ru.car.service.message.telegram.render.TelegramMessages;
import ru.car.service.message.telegram.router.CallbackData;
import ru.car.service.message.telegram.router.TelegramUpdateContext;
import ru.car.service.message.telegram.scene.SceneOutput;

import java.util.List;

import static org.assertj.core.api.Assertions.assertThat;
import static org.mockito.Mockito.verify;

@ExtendWith(MockitoExtension.class)
class ProfileSceneTest {

    @Mock UserService userService;
    @Mock NotificationSettingRepository settingRepository;

    ProfileScene scene;

    @BeforeEach
    void setUp() {
        scene = new ProfileScene(userService, settingRepository, messages());
    }

    @Test
    void keyIsProfile() {
        assertThat(scene.key()).isEqualTo("profile");
    }

    @Test
    void handle_open_rendersProfile() {
        User user = new User();
        user.setPhoneNumber("79313178898");
        user.setRole(Role.ROLE_USER);

        SceneOutput output = scene.handle(
            new CallbackData("profile", "open", List.of()),
            new TelegramUpdateContext(100L, 42L, user, null));

        assertThat(output.text()).contains("+7 931 317-88-98");
        assertThat(output.text()).contains("Пользователь");
    }

    @Test
    void handle_logoutConfirm_clearsTelegramLink() {
        SceneOutput output = scene.handle(
            new CallbackData("profile", "logout_confirm", List.of()),
            new TelegramUpdateContext(100L, 42L, null, null));

        verify(settingRepository).clearTelegramLink(42L);
        assertThat(output.text()).contains("отключены от Telegram").ignoringCase();
    }

    @Test
    void handle_deleteConfirm_callsUserServiceDelete() {
        SceneOutput output = scene.handle(
            new CallbackData("profile", "delete_confirm", List.of()),
            new TelegramUpdateContext(100L, 42L, null, null));

        verify(userService).deleteUser(42L);
        assertThat(output.text()).contains("Аккаунт удалён");
    }

    private static TelegramMessages messages() {
        var src = new ReloadableResourceBundleMessageSource();
        src.setBasename("classpath:i18n/telegram");
        src.setDefaultEncoding("UTF-8");
        src.setFallbackToSystemLocale(false);
        return new TelegramMessages(src);
    }
}
```

Note: we use existing `UserService.deleteUser(Long)` (found in step-2 of plan exploration). If signature returns something else — adjust.

- [ ] **Step 2: Create scene**

```java
package ru.car.service.message.telegram.scene.impl;

import lombok.extern.slf4j.Slf4j;
import org.springframework.stereotype.Component;
import org.telegram.telegrambots.meta.api.objects.replykeyboard.InlineKeyboardMarkup;
import org.telegram.telegrambots.meta.api.objects.replykeyboard.buttons.InlineKeyboardButton;
import ru.car.model.User;
import ru.car.repository.NotificationSettingRepository;
import ru.car.service.UserService;
import ru.car.service.message.telegram.render.TelegramMessages;
import ru.car.service.message.telegram.router.CallbackData;
import ru.car.service.message.telegram.router.TelegramUpdateContext;
import ru.car.service.message.telegram.scene.SceneOutput;
import ru.car.service.message.telegram.scene.TelegramScene;
import ru.car.util.MessageUtils;

import java.util.List;

@Slf4j
@Component
public class ProfileScene implements TelegramScene {

    public static final String KEY = "profile";
    public static final String ACTION_OPEN = "open";
    public static final String ACTION_LOGOUT = "logout";
    public static final String ACTION_LOGOUT_CONFIRM = "logout_confirm";
    public static final String ACTION_DELETE = "delete";
    public static final String ACTION_DELETE_CONFIRM = "delete_confirm";

    private final UserService userService;
    private final NotificationSettingRepository settingRepository;
    private final TelegramMessages messages;

    public ProfileScene(UserService userService, NotificationSettingRepository settingRepository, TelegramMessages messages) {
        this.userService = userService;
        this.settingRepository = settingRepository;
        this.messages = messages;
    }

    @Override
    public String key() {
        return KEY;
    }

    @Override
    public SceneOutput handle(CallbackData data, TelegramUpdateContext ctx) {
        return switch (data.action()) {
            case ACTION_OPEN -> render(ctx.user());
            case ACTION_LOGOUT -> renderConfirm(messages.get("tg.profile.confirm.logout"),
                "profile:logout_confirm", "profile:open");
            case ACTION_LOGOUT_CONFIRM -> {
                settingRepository.clearTelegramLink(ctx.userId());
                yield SceneOutput.editHtml(messages.get("tg.profile.logout.done"), null);
            }
            case ACTION_DELETE -> renderConfirm(messages.get("tg.profile.confirm.delete"),
                "profile:delete_confirm", "profile:open");
            case ACTION_DELETE_CONFIRM -> {
                try {
                    userService.deleteUser(ctx.userId());
                } catch (Exception e) {
                    log.error("deleteUser failed for {}", ctx.userId(), e);
                }
                yield SceneOutput.editHtml(messages.get("tg.profile.delete.done"), null);
            }
            default -> SceneOutput.noop();
        };
    }

    private SceneOutput render(User user) {
        String phone = user != null ? MessageUtils.formatPhone(user.getPhoneNumber()) : "-";
        String role = user != null && user.isAdmin()
            ? messages.get("tg.profile.role_admin")
            : messages.get("tg.profile.role_user");
        String body = messages.get("tg.profile.title") + "\n\n" +
            messages.get("tg.profile.body", phone, role);

        InlineKeyboardMarkup kb = new InlineKeyboardMarkup();
        kb.setKeyboard(List.of(
            List.of(btn(messages.get("tg.profile.btn.logout"), "profile:logout")),
            List.of(btn(messages.get("tg.profile.btn.delete"), "profile:delete")),
            List.of(
                btn(messages.get("tg.common.back"), "profile:back"),
                btn(messages.get("tg.common.home"), "home:open")
            )
        ));
        return SceneOutput.editHtml(body, kb);
    }

    private SceneOutput renderConfirm(String text, String yesCallback, String noCallback) {
        InlineKeyboardMarkup kb = new InlineKeyboardMarkup();
        kb.setKeyboard(List.of(
            List.of(btn(messages.get("tg.common.confirm.yes"), yesCallback)),
            List.of(btn(messages.get("tg.common.confirm.cancel"), noCallback))
        ));
        return SceneOutput.editHtml(text, kb);
    }

    private static InlineKeyboardButton btn(String text, String callback) {
        return InlineKeyboardButton.builder().text(text).callbackData(callback).build();
    }
}
```

- [ ] **Step 3: Run → pass**

- [ ] **Step 4: Commit**

```bash
git add backend/src/main/java/ru/car/service/message/telegram/scene/impl/ProfileScene.java \
        backend/src/test/java/ru/car/service/message/telegram/scene/impl/ProfileSceneTest.java
git commit -m "feat(tg-bot): ProfileScene — logout + delete with confirm dialogs"
```

---

### Task 18: `TemporaryQrScene` v2 — PNG with caption

**Files:**
- Modify: `backend/src/main/java/ru/car/service/message/telegram/scene/impl/TemporaryQrScene.java`
- Modify: `backend/src/test/java/ru/car/service/message/telegram/scene/impl/TemporaryQrSceneTest.java`

- [ ] **Step 1: Update test**

Replace `renderSuccess_buildsCreatedMessage` test with:

```java
@Test
void renderSuccess_returnsPhotoWithCaption() {
    UUID qrId = UUID.randomUUID();
    when(qrService.createTemporaryQr(1L)).thenReturn(QrDto.builder().qrId(qrId).build());

    SceneOutput output = scene.render(new TelegramUpdateContext(10L, 1L, null, null));

    assertThat(output.photo()).isNotNull();
    assertThat(output.caption()).contains(qrId.toString()).contains("car-id.ru/qr/" + qrId);
}
```

Also update error tests to expect text output (no photo):
```java
@Test
void renderAppException_returnsTextMessage() {
    when(qrService.createTemporaryQr(1L)).thenThrow(new AppException("Превышен лимит") {});
    // AppException is abstract, so use subclass or anonymous
    
    SceneOutput output = scene.render(new TelegramUpdateContext(10L, 1L, null, null));
    assertThat(output.text()).isEqualTo("Превышен лимит");
    assertThat(output.photo()).isNull();
}
```

Actually AppException is abstract — use `NotFoundException` or similar existing concrete subclass. Keep existing test pattern.

Also remove `HomeMenuScene` dependency from constructor — TemporaryQrScene no longer needs main keyboard (it returns photo, renderer sends it standalone; parent menu stays in chat).

Update `setUp`:
```java
@BeforeEach
void setUp() {
    TelegramMessages msgs = messages();
    scene = new TemporaryQrScene(qrService, msgs);
    ReflectionTestUtils.setField(scene, "url", "https://car-id.ru");
}
```

- [ ] **Step 2: Rewrite scene**

```java
package ru.car.service.message.telegram.scene.impl;

import lombok.extern.slf4j.Slf4j;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.core.io.Resource;
import org.springframework.stereotype.Component;
import org.telegram.telegrambots.meta.api.objects.InputFile;
import ru.car.dto.QrDto;
import ru.car.exception.AppException;
import ru.car.service.QrService;
import ru.car.service.message.telegram.render.TelegramMessages;
import ru.car.service.message.telegram.router.CallbackData;
import ru.car.service.message.telegram.router.TelegramUpdateContext;
import ru.car.service.message.telegram.scene.SceneOutput;
import ru.car.service.message.telegram.scene.TelegramScene;
import ru.car.util.QrUtils;

import java.io.IOException;

@Slf4j
@Component
public class TemporaryQrScene implements TelegramScene {

    public static final String KEY = "temp_qr";
    public static final String ACTION_CREATE = "create";

    private final QrService qrService;
    private final TelegramMessages messages;

    @Value("${swagger.server.url}")
    private String url;

    public TemporaryQrScene(QrService qrService, TelegramMessages messages) {
        this.qrService = qrService;
        this.messages = messages;
    }

    @Override
    public String key() {
        return KEY;
    }

    @Override
    public boolean canHandleText(String text) {
        return "Временный QR".equals(text);
    }

    @Override
    public SceneOutput render(TelegramUpdateContext ctx) {
        return create(ctx.userId());
    }

    @Override
    public SceneOutput handle(CallbackData data, TelegramUpdateContext ctx) {
        if (ACTION_CREATE.equals(data.action())) {
            return create(ctx.userId());
        }
        return SceneOutput.noop();
    }

    private SceneOutput create(Long userId) {
        try {
            QrDto qr = qrService.createTemporaryQr(userId);
            String qrUrl = url + "/qr/" + qr.getQrId();
            String caption = messages.get("tg.temp_qr.caption", qrUrl);
            Resource pngResource = QrUtils.generateQRCodeImage(qrUrl);
            InputFile input = new InputFile(pngResource.getInputStream(), "temp-qr-" + qr.getQrId() + ".png");
            return SceneOutput.photo(input, caption);
        } catch (AppException e) {
            return SceneOutput.sendHtml(e.getMessage(), null);
        } catch (IOException e) {
            log.error("Failed to build temp QR PNG", e);
            return SceneOutput.sendHtml(messages.get("tg.temp_qr.error"), null);
        } catch (Exception e) {
            log.error("внутренняя ошибка при создании временного кода", e);
            return SceneOutput.sendHtml(messages.get("tg.temp_qr.error"), null);
        }
    }
}
```

- [ ] **Step 3: Run tests**

Run: `cd backend && ./gradlew test --tests "ru.car.service.message.telegram.scene.impl.TemporaryQrSceneTest"`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add backend/src/main/java/ru/car/service/message/telegram/scene/impl/TemporaryQrScene.java \
        backend/src/test/java/ru/car/service/message/telegram/scene/impl/TemporaryQrSceneTest.java
git commit -m "feat(tg-bot): TemporaryQrScene v2 — PNG with caption"
```

---

### Task 19: `TelegramAuthorizationService` — welcome with ReplyKeyboardRemove + trigger Home

**Files:**
- Modify: `backend/src/main/java/ru/car/service/message/telegram/auth/TelegramAuthorizationService.java`
- Modify: `backend/src/test/java/ru/car/service/message/telegram/auth/TelegramAuthorizationServiceTest.java`
- Modify: `backend/src/main/java/ru/car/service/message/telegram/router/TelegramRouter.java`
- Modify: `backend/src/test/java/ru/car/service/message/telegram/router/TelegramRouterTest.java`

- [ ] **Step 1: Update TelegramAuthorizationService.welcome**

In `handle(long chatId, String text)`, change the welcome branch:

```java
settingRepository.updateTelegramDialogIdByUserId(user.get().getId(), chatId);
return SceneOutput.send(messages.get("tg.auth.welcome"),
    new org.telegram.telegrambots.meta.api.objects.replykeyboard.ReplyKeyboardRemove(true, null));
```

Remove `HomeMenuScene homeMenuScene` dependency from constructor (since we don't attach the old reply-keyboard anymore). This requires updating the constructor and the test. Check if `ReplyKeyboardRemove` constructor takes `(true, null)` or just `(true)` — depends on version. Probably builder is safer:

```java
org.telegram.telegrambots.meta.api.objects.replykeyboard.ReplyKeyboardRemove remove =
    org.telegram.telegrambots.meta.api.objects.replykeyboard.ReplyKeyboardRemove.builder()
        .removeKeyboard(true).build();
return SceneOutput.send(messages.get("tg.auth.welcome"), remove);
```

- [ ] **Step 2: Update test**

In `TelegramAuthorizationServiceTest.validPhoneAndUserFound_bindsAndReturnsWelcome`:

```java
@Test
void validPhoneAndUserFound_bindsAndReturnsWelcomeWithKeyboardRemove() {
    User user = new User();
    user.setId(42L);
    when(settingRepository.existsByTelegramDialogId(100L)).thenReturn(false);
    when(userService.findByPhoneNumber("79001234567")).thenReturn(Optional.of(user));

    SceneOutput output = service.handle(100L, "+79001234567");

    verify(settingRepository).updateTelegramDialogIdByUserId(42L, 100L);
    assertThat(output.text()).contains("Рады приветствовать");
    assertThat(output.replyKeyboard()).isInstanceOf(
        org.telegram.telegrambots.meta.api.objects.replykeyboard.ReplyKeyboardRemove.class);
}
```

Also update setUp — remove HomeMenuScene if was added previously:
```java
@BeforeEach
void setUp() {
    service = new TelegramAuthorizationService(userService, settingRepository, messages());
}
```

Update constructor of `TelegramAuthorizationService`:
```java
public TelegramAuthorizationService(UserService userService,
                                     NotificationSettingRepository settingRepository,
                                     TelegramMessages messages) {
    this.userService = userService;
    this.settingRepository = settingRepository;
    this.messages = messages;
}
```

Remove field `homeMenuScene`.

- [ ] **Step 3: Router — after successful auth, dispatch Home as separate message**

In `TelegramRouter.route(Update)`, after auth branch:

```java
if (!settingRepository.existsByTelegramDialogId(chatId)) {
    String text = extractAuthText(update);
    SceneOutput output = authService.handle(chatId, text);
    renderer.dispatch(output, chatId, null);
    // if auth just succeeded (chatId now linked), render Home as separate message
    if (settingRepository.existsByTelegramDialogId(chatId)) {
        Long userId = settingRepository.findUserIdByTelegramDialogId(chatId);
        User user = userService.getUserOrThrowNotFound(userId);
        TelegramUpdateContext ctx = new TelegramUpdateContext(chatId, userId, user, update);
        sceneRegistry.findByKey("home").ifPresent(home -> {
            SceneOutput homeOutput = home.render(ctx);
            renderer.dispatch(homeOutput, chatId, null);
        });
    }
    return;
}
```

- [ ] **Step 4: Update router test**

In `TelegramRouterTest`, add:

```java
@Test
void afterSuccessfulAuth_rendersHomeSceneAsSecondMessage() {
    Update update = contactUpdate(100L, "+79001234567");
    // first call: not linked; authService.handle will succeed; second call: linked
    when(settingRepository.existsByTelegramDialogId(100L)).thenReturn(false, true);
    when(authService.handle(100L, "+79001234567")).thenReturn(SceneOutput.send("welcome", null));
    when(settingRepository.findUserIdByTelegramDialogId(100L)).thenReturn(42L);
    when(userService.getUserOrThrowNotFound(42L)).thenReturn(new User());
    TelegramScene homeScene = mock(TelegramScene.class);
    when(sceneRegistry.findByKey("home")).thenReturn(Optional.of(homeScene));
    when(homeScene.render(any())).thenReturn(SceneOutput.sendHtml("home", null));

    router.route(update);

    verify(homeScene).render(any());
    verify(renderer, times(2)).dispatch(any(SceneOutput.class), eq(100L), any());
}
```

Add `import static org.mockito.Mockito.times;`.

- [ ] **Step 5: Run all tests**

Run: `cd backend && ./gradlew test --tests "ru.car.service.message.telegram.*"`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add backend/src/main/java/ru/car/service/message/telegram/auth/TelegramAuthorizationService.java \
        backend/src/main/java/ru/car/service/message/telegram/router/TelegramRouter.java \
        backend/src/test/java/ru/car/service/message/telegram/auth/TelegramAuthorizationServiceTest.java \
        backend/src/test/java/ru/car/service/message/telegram/router/TelegramRouterTest.java
git commit -m "feat(tg-bot): auth welcome uses ReplyKeyboardRemove + auto-render Home"
```

---

### Task 20: Router — handle `photo` in SceneOutput from scenes + remove HomeMenuScene usage

**Files:**
- Modify: `backend/src/main/java/ru/car/service/message/telegram/router/TelegramRouter.java`

- [ ] **Step 1: Replace `dispatchUnknown` to use HomeScene instead of HomeMenuScene**

In `TelegramRouter.java` find `dispatchUnknown`:

```java
private void dispatchUnknown(TelegramUpdateContext ctx) {
    sceneRegistry.findByKey("home").ifPresent(scene -> {
        if (scene instanceof ru.car.service.message.telegram.scene.impl.HomeScene home) {
            SceneOutput output = home.renderUnknown(ctx);
            renderer.dispatch(output, ctx.chatId(), null);
        }
    });
}
```

Remove `HomeMenuScene` field from TelegramRouter constructor (if still present). Update constructor:

```java
public TelegramRouter(NotificationSettingRepository settingRepository,
                      UserService userService,
                      SceneRegistry sceneRegistry,
                      TelegramAuthorizationService authService,
                      TelegramRenderer renderer) {
    // ...
}
```

- [ ] **Step 2: Update router test**

In `TelegramRouterTest.java` remove `@Mock HomeMenuScene homeMenuScene` field and its setup. Rework `unknownCallback_fallsBackToHomeUnknown` and `unknownText_fallsBackToHomeUnknown` to use `HomeScene` from registry:

```java
@Test
void unknownText_fallsBackToHomeUnknown() {
    Update update = textUpdate(100L, "random text");
    mockAuthorized(100L, 42L);
    when(sceneRegistry.findByText("random text")).thenReturn(Optional.empty());
    HomeScene homeScene = mock(HomeScene.class);
    when(sceneRegistry.findByKey("home")).thenReturn(Optional.of(homeScene));
    when(homeScene.renderUnknown(any())).thenReturn(SceneOutput.sendHtml("Неизвестная команда", null));

    router.route(update);

    verify(homeScene).renderUnknown(any());
}
```

Similarly for callback branch.

Import `ru.car.service.message.telegram.scene.impl.HomeScene`.

- [ ] **Step 3: Build + test**

Run: `cd backend && ./gradlew test --tests "ru.car.service.message.telegram.router.TelegramRouterTest"`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add backend/src/main/java/ru/car/service/message/telegram/router/TelegramRouter.java \
        backend/src/test/java/ru/car/service/message/telegram/router/TelegramRouterTest.java
git commit -m "refactor(tg-bot): router delegates unknown-command to HomeScene.renderUnknown"
```

---

### Task 21: Delete obsolete `HomeMenuScene` + test

**Files:**
- Delete: `backend/src/main/java/ru/car/service/message/telegram/scene/impl/HomeMenuScene.java`
- Delete: `backend/src/test/java/ru/car/service/message/telegram/scene/impl/HomeMenuSceneTest.java`

- [ ] **Step 1: Check no remaining references**

Run:
```bash
cd /Users/kirillreshetov/IdeaProjects/car-id-claude
grep -rn "HomeMenuScene" backend/src/
```
Expected: only in files being deleted. If any other reference found — go back and update.

- [ ] **Step 2: Delete files**

```bash
git rm backend/src/main/java/ru/car/service/message/telegram/scene/impl/HomeMenuScene.java
git rm backend/src/test/java/ru/car/service/message/telegram/scene/impl/HomeMenuSceneTest.java
```

- [ ] **Step 3: Build + test**

Run: `cd backend && ./gradlew compileJava compileTestJava test`
Expected: SUCCESS.

- [ ] **Step 4: Commit**

```bash
git commit -m "refactor(tg-bot): delete obsolete HomeMenuScene (replaced by HomeScene)"
```

---

### Task 22: Full coverage + CarIdApplicationTests + grep checks

**Files:** validation only (no code changes expected).

- [ ] **Step 1: Run full test suite**

Run: `cd backend && ./gradlew test`
Expected: all PASS.

- [ ] **Step 2: CarIdApplicationTests**

Already covered by full test run. Explicitly:

Run: `cd backend && ./gradlew test --tests "ru.car.carId.CarIdApplicationTests"`
Expected: PASS.

- [ ] **Step 3: Grep — no Russian strings in .java**

Run: `grep -rn "[а-яА-Я]" backend/src/main/java/ru/car/service/message/telegram/ | grep -v '//\|\*\|log\.'`

Expected: empty or only JavaDoc/log messages. User-facing Russian strings — в properties.

- [ ] **Step 4: Grep — no @Setter in telegram**

Run: `grep -rn "@Setter\|import lombok.Setter" backend/src/main/java/ru/car/service/message/telegram/`
Expected: empty.

- [ ] **Step 5: Grep — no NotificationRepository in telegram**

Run: `grep -rn "NotificationRepository" backend/src/main/java/ru/car/service/message/telegram/`
Expected: empty.

- [ ] **Step 6: Report coverage (visual inspection)**

Check that each new scene has a test file; each new service method has a unit test; `TelegramRendererTest` covers `dispatchPhoto`.

- [ ] **Step 7: No commit needed if all green.**

---

### Task 23: Docs close

**Files:**
- Modify: `ai-ru/review/2026-04-21_TELEGRAM_EPIC.md` — строка 2.2 ✅
- Modify: `ai-ru/features/F8_TELEGRAM_BOT.md` — обновить раздел «Реализация» + сценарии
- Modify: `ai-ru/backlog/BF1_TELEGRAM_BOT_UI.md` — пометить 2.2 закрытым

- [ ] **Step 1: Epic**

In `ai-ru/review/2026-04-21_TELEGRAM_EPIC.md` change:
```
| **2.2** 🚧 | ... | ... | [spec](2026-04-21_TG_2.2_BASIC_SCREENS.md) |
```
to:
```
| **2.2 ✅** | ... | ... | [spec](2026-04-21_TG_2.2_BASIC_SCREENS.md) · [plan](2026-04-21_TG_2.2_PLAN.md) |
```

- [ ] **Step 2: F8**

Update `F8_TELEGRAM_BOT.md`:
- Раздел «Реализация» — добавить новые сцены (Home, QrDetails, NotificationList, Settings, Profile).
- «Команды-кнопки» таблица — обновить перечень callback-форматов.
- Сценарий «Админ-меню» — уже удалён в 2.1; ничего не делать.
- Новые сценарии: «Главное меню», «Детали метки», «Список уведомлений», «Настройки каналов», «Профиль» — добавить под «Сценарии».
- Статус: `**Статус:** ✅ В проде · **Последний апдейт:** 2026-04-21 (Phase 2.2 базовые экраны)`.

- [ ] **Step 3: BF1**

В `ai-ru/backlog/BF1_TELEGRAM_BOT_UI.md` обновить header note:
```
> **✅ Под-спеки 2.1 + 2.2 закрыты 2026-04-21.** Следующий — 2.3 (ReportEvent/Support/Marketplace).
```

- [ ] **Step 4: Commit**

```bash
git add ai-ru/review/2026-04-21_TELEGRAM_EPIC.md \
        ai-ru/features/F8_TELEGRAM_BOT.md \
        ai-ru/backlog/BF1_TELEGRAM_BOT_UI.md
git commit -m "docs(tg-bot): close Phase 2.2 — basic screens"
```

---

## Self-Review

**Spec coverage:**
- 6 scenes ✓ (tasks 12–17 + 18)
- HTML + эмодзи ✓ (i18n task 5 + scenes use parseMode=HTML)
- Back navigation via parentKey ✓ (task 11)
- Edit-in-place ✓ (scenes return SceneOutput.editHtml)
- PNG for QRs ✓ (tasks 3, 4 transport/renderer; tasks 14, 18 scenes use photo())
- ReplyKeyboardRemove after welcome ✓ (task 19)
- New service methods ✓ (tasks 6, 7, 8, 9, 10)
- Legacy text-triggers preserved ✓ (QrListScene.canHandleText + TemporaryQrScene.canHandleText)

**Placeholder scan:** no TBD / vague instructions found.

**Type consistency:** `TelegramScene.key()`/`parentKey()` consistent; `SceneOutput` factories consistent; callback formats consistent (all `scene:action:args`).

**Ambiguity fixes applied inline:** QrRepository save vs update (tasks mention adapting to actual API), NotificationSettingService constructor field name, etc.

---

## Execution Handoff

Plan complete and saved to `ai-ru/review/2026-04-21_TG_2.2_PLAN.md`.

**Subagent-driven-development** (per user direction: autonomous execution to end).
