# Реквизиты подключения для deploy/deploy-frontend.sh.
# Скопируй в deploy/frontend.env и подставь значения из ops.inf.
# deploy/frontend.env в .gitignore — в репозиторий не попадёт.

CAR_ID_SSH_USER=root
CAR_ID_SSH_HOST=79.174.94.70
# CAR_ID_SSH_PORT=22
# CAR_ID_SSH_KEY=~/.ssh/car-id

# Каталог фронта на сервере (как в GitHub-workflow)
# CAR_ID_FRONT_TARGET=/var/www/car-id/car-id-front

# Удалять на сервере файлы, которых нет локально (осторожно): 1 — да
# CAR_ID_FRONT_DELETE=0
